# Apex AI Backend — Variables d'environnement
# Copie ce fichier en .env (non commit) et remplis les valeurs

# === OBLIGATOIRE ===
ANTHROPIC_API_KEY=sk-ant-apiXXXXXX
JWT_SECRET=change-me-to-a-random-string-min-32-chars

# === FALLBACK MULTI-PROVIDER (recommandé) ===
OPENAI_API_KEY=sk-proj-XXXXXX  # fallback si Anthropic down

# === GITHUB PROXY (contourne Safari PWA CORS) ===
GITHUB_PAT=github_pat_11XXXXXXX  # PAT Kevin avec Contents R/W
ALLOWED_REPOS=9r4rxssx64-creator/CMCteams

# === SUPABASE (Postgres + Auth) ===
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_KEY=eyJhbGciXXXXXX  # anon public key
SUPABASE_SERVICE_KEY=eyJhbGciXXXXXX  # service_role key (server-only)

# === PINECONE (Vector KB) ===
PINECONE_API_KEY=pcsk-XXXXXX
PINECONE_INDEX=apex-kb

# === EMAIL ===
SENDGRID_API_KEY=SG.XXXXXX  # ou utilise EmailJS via route dédiée

# === CORS ===
ALLOWED_ORIGINS=https://9r4rxssx64-creator.github.io,http://localhost:3000
