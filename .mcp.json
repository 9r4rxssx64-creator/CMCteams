{
  "_comment": "MCP servers du repo — chargés à chaque session Claude Code web. SEO: nanobanana + firecrawl (${ENV_VAR}). github = serveur MCP distant : le credential est fourni AUTOMATIQUEMENT par le Coffre d'identifiants <platform.claude.com> matché sur l'URL <api.githubcopilot.com> (PAS de header ici, sinon ça casse l'injection).",
  "mcpServers": {
    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp/"
    },
    "nanobanana-mcp": {
      "command": "npx",
      "args": ["-y", "@ycse/nanobanana-mcp@1.1.1"],
      "env": { "GOOGLE_AI_API_KEY": "${GEMINI_API_KEY}" }
    },
    "firecrawl-mcp": {
      "command": "npx",
      "args": ["-y", "firecrawl-mcp@3.11.0"],
      "env": { "FIRECRAWL_API_KEY": "${FIRECRAWL_API_KEY}" }
    }
  }
}
