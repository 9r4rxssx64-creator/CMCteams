name = "kdmc-crea-ai"
main = "worker.js"
compatibility_date = "2024-11-01"

# Worker IA de Créa Studio (détourage / cartoon / upscale via Replicate).
# Servi sur https://kdmc-crea-ai.9r4rxssx64.workers.dev (sous-domaine du compte).
# Le secret REPLICATE_API_TOKEN est injecté par le workflow deploy-kdmc-crea-ai.yml
# (depuis AX_REPLICATE_KEY) — jamais dans ce fichier ni côté client.
workers_dev = true
