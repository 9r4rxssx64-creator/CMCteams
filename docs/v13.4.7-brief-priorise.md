# Apex v13.4.7+ — Brief priorisé (Kevin 2026-05-10)

**Engagement honnêteté Kevin** : tests RÉELS uniquement, notes RÉELLES, JAMAIS régression, JAMAIS rien casser, autonomie totale, travail en concert avec Apex IA.

## P0 — Reconnaissance admin Kevin + Wake word (CRITIQUE)

Kevin signale : "Apex me demande très souvent que c'était des actions admin"
+ "quand je dis apex, il ne se passe rien".

### P0.1 — Auth admin Kevin reconnaissance permanente
**Règle CLAUDE.md déjà documentée** (Kevin 2026-04-26 ABSOLUE) :
- Aliases acceptés : "Kevin DESARZENS", "DESARZENS Kevin", "Kevin", "DESARZENS", "kevin.desarzens@gmail.com", "K DESARZENS", "KD", "KDMC"
- PIN admin : 200807 (déjà spec, hash PBKDF2 200k)
- Test : si Kevin tape n'importe quel alias + PIN admin → reconnu kdmc_admin

**Bug terrain** : Apex redemande "est-ce action admin" trop souvent.
**Fix** : `_isKevinAdmin(name)` doit retourner true STABLE pour tout alias + PIN.
Une fois reconnu, session admin garde l'état AID jusqu'au logout.

**Tests régression OBLIGATOIRES** :
- 8 aliases × 1 PIN = 8 logins → tous reconnus admin
- Session timeout 8h respecté
- Reload PWA → session admin préservée si dans TTL
- Test : Kevin tape "Modifie X" → action admin sans re-demande PIN

### P0.2 — Wake word "Dis Apex" 
**Bug terrain** : Kevin dit "Dis Apex" → rien se passe.

**Causes probables** :
1. Permission micro non accordée (iOS Safari)
2. SpeechRecognition `continuous=false` sur iOS = stop après 15-30s
3. Wake word service `_axWakeRecognition` plantage silencieux
4. Voiceprint Kevin pas enrôlé (Erreur #1 wake word per-user)

**Fix audit** :
- services/voice/wake-word.ts (si existe) ou créer
- Test runtime micro permission + recognition active
- Notif claire si permission denied : "🎙 Permission micro requise. Active dans Réglages iOS."
- Toast "👂 J'écoute" quand wake word activé
- Sentinelle wake-word-watch (1×/h) vérifie recognition still alive

**Tests régression** :
- "Dis Apex" dans test sandbox → activation détectée
- Permission micro denied → message clair
- 6 variantes ("dis apex", "Dis Apex", "DIS APEX", "okay apex", "hey apex", "ok apex")

## P1 — Compte Laurence ❤️

Kevin demande : "vérifie aussi le compte de Laurence, sa reconnaissance, son code PIN, qui ne fonctionnait pas correctement".

### P1.1 — PIN Laurence
**Erreur #37 documentée** : `ax_pin` global écrasé quand Laurence change son PIN.
**Fix v12.240 déjà appliqué** mais à vérifier runtime :
- `ax_pin_<laurence_uid>` distinct de `ax_pin` (admin global)
- `axChangePin(userId, hash)` guard `if userId===ADMIN_ID write ax_pin else write ax_pin_<uid>`
- Audit log toute écriture vault

**Tests régression** :
- Laurence change PIN → `ax_pin_<laurence_uid>` mis à jour, `ax_pin` PRÉSERVÉ
- Kevin login après → toujours admin (PIN intact)
- Laurence login avec son PIN → reconnue Laurence (tier laurence)
- Laurence login avec PIN Kevin → REFUSÉ

### P1.2 — Reconnaissance nom Laurence
- Aliases acceptés : "Laurence SAINT-POLIT", "SAINT-POLIT Laurence", "Laurence", etc.
- Compte pré-configuré `laurence` ou similaire dans PRECONFIGURED_USERS
- Tier `laurence` avec permissions tiered (auto/notify/validate)

**Tests régression** :
- 6 aliases Laurence → reconnue
- Permissions tiered respectées (peut faire ses actions, demande pour validate)
- Mode admin Kevin actif dans vue Laurence (cf règle CLAUDE.md)

## P2 — UX professionnel expert entreprise (RUNTIME RÉEL)

Kevin demande : "u x professionnel expert entreprise, commerce" — niveau Claude.ai / ChatGPT.

### P2.1 — Audit runtime iPhone réel (Apex teste Apex)
- Apex IA via apex-execute lance Playwright simulator iPhone 15 Pro 393×852
- Capture screenshots de chaque vue critique : login, chat, coffre, admin, settings
- Vérifie touch targets 44px partout
- Vérifie pas de scroll horizontal sauf nav explicite
- Mesure LCP / INP / CLS

### P2.2 — Greeting + espace vide + chat input
- Greeting collapse si messages > 0
- 4 chips suggestion cliquables état vide
- Input bar compact + submit ↑ intégré
- Footer green-dot 4px discret

### P2.3 — Animation et fluidité
- Streaming token-par-token fluide (pas saccade)
- Auto-scroll smooth pendant tokens
- Modal Apple half-sheet
- Skeleton screens loading

**Tests régression** :
- Lighthouse mobile ≥ 95
- axe-core 0 violations critiques
- Touch test 5 viewports (375/390/412/768/1200)

## P3 — Tests en concert avec Apex (cross-app)

Kevin demande : "travail de concert avec apex, pour le faire tester en live".

### P3.1 — apex-execute self-testing
- Apex IA lance auto-test-everything.runFullHealthCheck()
- Score global réel mesuré (pas inventé)
- Si <100%, retry 3× + escalade ax_claude_todo

### P3.2 — Tests E2E Kevin + Laurence + clients
- Playwright headless cloud (BrowserStack si Kevin OK budget)
- Scénarios :
  1. Kevin login (Kevin + 200807) → admin reconnu
  2. Kevin tape "Dis Apex" → wake word activé
  3. Kevin colle clé Anthropic → stockée chiffrée
  4. Laurence login (Laurence + son PIN) → vue Lite
  5. Client free login → vue restreinte
- Rapport email Kevin si fail

## P4 — 8 bugs restants v13.4.6 (par priorité d'impact)

| # | Bug | Effort |
|---|-----|--------|
| P4.1 | Chat persistence Firebase (continue recommence à zéro) | 5h |
| P4.2 | setInterval cleanup central + abort registry | 3h |
| P4.3 | Photo upload affichage inline + IA context | 4h |
| P4.4 | localStorage direct 10 services → wrapper sync | 4h |
| P4.5 | innerHTML escapeHtml 10+ fichiers | 2h |
| P4.6 | .then() catch + 7 catch silencieux | 2h |
| P4.7 | UX greeting + viewport strict | 2h |
| P4.8 | SW iOS force compare APP_VER 2min | 2h |

## Méthodologie OBLIGATOIRE pour chaque fix v13.4.7+

1. **Audit RUNTIME d'abord** (pas juste static grep)
2. **Test régression AVANT push** (existing tests + nouveau test fix)
3. **Score mesuré** (jamais inventé)
4. **Honnêteté brute** (si bug pas fixé, dire "pas fixé", pas "fixé conditionnel")
5. **Anti-régression strict** (snapshot Git avant + rollback si fail)
6. **Cohérence triple** data-app-ver source = build = deploy (Erreur #54)
7. **Update docs racine** AVANT push (MEMO_RESUME, KEVIN_INVENTORY)
8. **Notif Kevin** Telegram si bloqué après 3× retry

## Garanties Kevin

- JAMAIS de régression (engagement permanent CLAUDE.md règle absolue)
- JAMAIS rien casser (snapshot Git + tests existing)
- Autonomie totale (pas demander Kevin si peut faire seul)
- Tests RÉELS uniquement (pas estimés)
- Scores RÉELS uniquement (mesurés, pas inventés)
- Apex en concert (apex-execute self-test obligatoire)
- Stop net si quota Anthropic atteint (notif Telegram + résumé)

## Ordre d'exécution suggéré

Quand quota Anthropic OK + Kevin donne GO :

1. v13.4.7 = P0.1 + P0.2 (admin Kevin + wake word) ~6h
2. v13.4.8 = P1 (Laurence) ~4h
3. v13.4.9 = P4.1 + P4.2 (chat persistence + setInterval) ~8h
4. v13.4.10 = P4.3 + P4.4 (photo + localStorage refactor) ~8h
5. v13.4.11 = P4.5 + P4.6 + P4.7 + P4.8 (XSS + .then + UX + SW iOS) ~8h
6. v13.4.12 = P2 + P3 (UX runtime + tests cross-app) ~10h
7. v13.5.0 = release candidate (score réel mesuré, audit externe optionnel)

**Total estimé : 44h sur 5-7 sessions**
**Coût tokens estimé : 250-400k tokens (Sonnet 4.6) selon complexité**
**Cap recommandé : 100k tokens / session, stop net si dépasse**

---

*Brief préparé 2026-05-10 par Claude Code. À exécuter par subagent v13.4.7 ou Apex Continuator Senior (agent Console Kevin) selon disponibilité quota.*
