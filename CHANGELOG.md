# CHANGELOG — CMCteams + Apex AI

Historique complet des versions. Les 5 dernières versions restent dans `CLAUDE.md` pour référence rapide.

---

## Apex v13.0.73 → v13.0.77 (2026-05-04 — MEGA SPRINT FINAL)

**Session 2026-05-04 PM** : 17 subagents en parallèle, 5 commits poussés.

### v13.0.73 — Fix critique CSP iPhone + boutons admin/footer
- Fix CSP iPhone Safari PWA (boutons admin et footer non-cliquables)
- Inline scripts → externes avec nonce
- Validation 100% boutons opérationnels

### v13.0.74 — MEGA SPRINT subagents (8 livraisons)
- **Browser fix blank + boost** : 95 tests, fallback Archive/Reader/Cache/Safari
- **Voix 61** : 21 PRO + 20 FUN + 20 thématiques + 12 effets WebAudio (53 tests)
- **Tools IA 105** : 12 catégories (71 tests)
- **Sentinelles 22** : auto-fix 3x + escalade Firebase (80 tests)
- **Skills experts 15+** : .claude/skills/ (4712 lignes documentation)

### v13.0.75 — 5 vues P0 + parité Claude Code 100%
- **5 vues P0 livrées** : Dashboard / Vault / KB / Toolbox / SelfDiag (107 tests, 1761 lignes UI)
- **Apex parité Claude Code** : services/apex-claude-code-parity.ts (29 méthodes Read/Edit/Write/Bash/Web/Subagent/MCP/Self-*, 97 tests)
- **Apex auto-modification** : services/apex-execute.ts étendu (23 tasks whitelist, 12 forbidden, 138 tests)

### v13.0.76 — MEGA SPRINT FINAL (parité ~95%)
- **5 modules pro EXPERT boost** : cuisine 41 recettes, medical 38 médocs, finance IS/TVA/successions, legal 25 codes, translator 56 langues (86 tests)
- **5 studios manquants ajoutés** : Logo / Présentation / Préfecture / Clip / Photo (~2300L, 137 tests)
- **5 studios boost MAX** : music / video / cv / invoice / contract (+1614L, 198 tests)
- **3 modules pro stubs** : Business / Education / Certifications (~1250L, 89 tests)

### v13.0.77 — Liens recharge MAX + ON/OFF toggles + Preflight
- **services/preflight.ts** : preflight check tools/modules avant présentation (94.51% cov, 66+35 tests)
- **services/feature-toggles.ts** : 109 features ON/OFF global + per-user (98.23% cov, 80 tests + UI admin)
- **services/links-registry.ts** : 51 services avec dashboard/billing/docs/support/status/api/usage (53 tests)
- **services/vault-triple-persist.ts** : localStorage + IDB + Firebase FB_FIX (23 tests)

### Stats finales v13.0.77
- **TS strict** : 0 errors
- **ESLint** : 0 errors, 0 warnings (--max-warnings=0)
- **Tests** : 4463+ passing, 9 skipped, 0 fail
- **Build** : 2.23s
- **Coverage** : ≥85%

### Parité v12 vs v13
| Domaine | v12 | v13.0.77 | Statut |
|---------|----:|---------:|--------|
| Vues P0 | 100% | 85% | 🟢 |
| Studios | 15 | 10 | 🟡 |
| Modules pro | 8 | 8 | ✅ |
| Voix | 50 | 61 | ✅ |
| Tools IA | 100+ | 105 | ✅ |
| Sentinelles | 13 | 22 | ✅ 170% |
| Skills | 0 | 15 | ✅ NEW |

### 5 règles permanentes Kevin ajoutées
1. TOUT AU MAX TOUJOURS
2. APEX = MÊME ACCÈS QUE CLAUDE CODE
3. APEX VÉRIFIE FONCTIONNEMENT AVANT PRÉSENTER (preflight)
4. BOUTONS ON/OFF GÉNÉRAL + INDIVIDUEL
5. 100/100 RÉEL CHAQUE AXE

---

## v9.396 — Sentry monitoring intégré (17 avril 2026)

- Ajout du **Sentry Loader Script** dans `<head>` d'index.html — monitoring erreurs production gratuit (5k events/mois).
- CSP v9.45 **v9.396** : élargissement de `script-src` et `connect-src` pour autoriser `*.sentry-cdn.com` et `*.ingest.sentry.io`.
- Preconnect + DNS prefetch Sentry ajoutés pour latence réduite.
- Organisation Sentry : `kdmc` — Projet : `cmcteams` (Browser JavaScript, vanilla JS).
- Lazy-load : le SDK Sentry ne se charge que si une erreur est capturée (impact perf nul sur page chargement).
- Fait suite à résolution P1 (workflow cron GitHub Actions mergé PR #82) + P2 (chat_id Telegram validé, bot opérationnel).

---

## v9.203 → v9.246 — Session 2026-04-17 bis (44 versions, géoloc + pit boss + heures effectives + notifs temps réel)

### 📣 Batch 15 — Export + alertes + docs (v9.244 → v9.246)
- **v9.246** : MAJ CLAUDE.md + CHANGELOG.md (bump v9.232 → v9.246)
- **v9.245** : `pitLowAckEmps(threshold)` + bloc alerte dashboard "<70% conformité"
- **v9.244** : `exportPitAckCSV(sinceH)` — export stats ack pour audit RH (matricule, nom, envoyés, acquittés, taux, délai, types)

### 📊 Batch 14 — Auto-rotation + dashboard pit boss (v9.241 → v9.243)
- **v9.243** : `vPitDashboard()` — 4 KPIs + toggle auto-rotation + tableau par emp avec code couleur
- **v9.242** : `pitAckStats(sinceMs)` + `_fmtDelay(ms)` — stats envoyés/acquittés/délai/taux
- **v9.241** : `autoRotationCron()` toutes 60s + opt-out `cmc_auto_rotation_off`

### 📱 Batch 13 — Notifs emp temps réel (v9.233 → v9.240)
- **v9.240** : Rotation broadcast → notif à tous les emps sur la table
- **v9.239** : Bannière in-app `_checkPitBanner()` + animation `pitBannerSlide` + TTS
- **v9.238** : `pitAckMessage(ts)` + `pitPendingAcks()` + badge vPit header
- **v9.237** : Convocations groupées (uids, group "present"/"all"/"team:X") + `_pitConvocationPrompt`
- **v9.236** : `pitAction("convocation")` 6 presets (visite_medicale, habillement, rh, formation, pitboss, annonce)
- **v9.235** : `_notifFinService(uid)` → "👋 Fin de service"
- **v9.234** : `_notifBreak(uid, fromTid)` → "☕ Break (table X) · ~20 min"
- **v9.233** : `_notifAssign(uid, tid)` + `sendPitMessage()` → "🎲 Assigné : table X · Jeu Y · ~Z min"

### 🎰 Batch 12 — Historique ghost + notif (v9.230 → v9.232)
- **v9.232** : MAJ CLAUDE.md + CHANGELOG.md (traces session) + FB_LOCAL `cmc_ghost_log`
- **v9.231** : `getGhostHistoryForUid()`, `ghostHistoryCountLast24h()`, store `cmc_ghost_log` rolling 500
- **v9.230** : Ghost watcher polling 30s + notif push persistent > 2min (sendNotif tag `ghost-<uid>`)

### 🏆 Batch 11 — Coord tables ↔ pit boss (v9.228 → v9.229)
- **v9.229** : Ghost alerts dans `pitCompute` (emps présents détectés à coord physique liée sans être assignés)
- **v9.228** : `pitAction("linkPhys",...)` + bouton 📍 par table live + `_pitLinkPhysPrompt()`

### 📊 Batch 10 — CSV export + dashboard heures (v9.225 → v9.227)
- **v9.227** : `vDashboardHeures()` mensuel (KPIs globaux + barre progression vs contractuel)
- **v9.226** : Auto-assign GPS dans `pitAction.openTable` (priorité 1 physTableId)
- **v9.225** : `exportEffectiveHoursCSVDay/Month` avec récap mensuel par employé

### 🕰 Batch 9 — Heures effectives + coord tables (v9.223 → v9.224)
- **v9.224** : `calcEffectiveHours(uid, ts)` via timings (pre/post/pause/coupure 16/3 auto-détectée)
- **v9.223** : `cmc_table_coords` + `detectEmpAtTable()` + `vTablesCoords` + bouton "Capturer ma position"

---

## v9.153 → v9.202 — Session 2026-04-17 (50 versions, 12 audits externes, note 6.62→8.50)

### 🏆 Batch 8 — Polish B+C+D post-audits (v9.199 → v9.202)
- **v9.202** : Audit log accès clear passwords + expiry check 180j (NIST 800-63B)
- **v9.201** : Validation stricte inputs `saveOv` (eid, day, code whitelist CK)
- **v9.200** : LRU cache helper + memoize `getOnlineUsers` TTL 5s
- **v9.199** : `alt=""` images + `aria-label="Fermer"` sur 9 boutons ✕ modales

### 🌟 Batch 7 — Modules niche SBM (v9.195 → v9.198)
- **v9.198** : Bulletin de paie pré-visualisé unifié (fixe + cagnotte + %CA + jours)
- **v9.197** : Multi-Casino SBM (CMC + CDP + Sun + Monte-Carlo Bay)
- **v9.196** : Module Cagnottes & %CA Convention Article 13 (8 jeux, répartition)
- **v9.195** : Module Événements Monaco (GP F1, EPT, Tennis, Festival, Fête Nat.)

### 📝 Batch 6 — Audits externes + améliorations (v9.190 → v9.194)
- **v9.194** : `aria-hidden` sur dots décoratifs + AUDIT_EXTERNE consolidé 10 audits
- **v9.193** : Indexes O(1) `_empsById`/`_teamsById`/`_empsTeamIndex` + ARIA + schema validation
- **v9.192** : aria-pressed sur filter chips + alt images
- **v9.191** : `_rebuildIndexes()` au boot + fbApplyData + sav()
- **v9.190** : Fix contrastes #5a6a50 → #8aa088 + AUDIT_EXTERNE initial 5 agents

### 💎 Batch 5 — Vers 10/10 (v9.184 → v9.189)
- **v9.189** : Keyboard navigation cellules (arrow/Enter/Del/Space)
- **v9.188** : Fix checksum stable (tri récursif des clés)
- **v9.187** : Rate limits submitLeaveRequest + timeclockPunch
- **v9.186** : Calcul paie SBM + indice Monaco historique (10+ sources)
- **v9.185** : Snapshots planning (15 rotatifs) + checksum + règle 10 sources
- **v9.184** : PWA install prompt natif + Web Share API

### 🌟 Batch 4 — Premium novateur (v9.174 → v9.183)
- **v9.183** : Deep-links URL `?y=&m=&v=` + bouton 🔗 FAB
- **v9.182** : Bouton "Appliquer à la colonne famille" dans picker
- **v9.181** : Copier planning du mois précédent (admin batch)
- **v9.180** : Export CSV pointages personnels 30j
- **v9.179** : Carte "Prochain service" contextuelle vAccueil
- **v9.178** : Quick profile modal (aperçu emp 420px)
- **v9.177** : Historique pointages 7 jours + barres progression
- **v9.176** : Team insights cards vStats (3 métriques)
- **v9.175** : Cheatsheet raccourcis clavier (modal 4 sections)
- **v9.174** : Range select Shift+Click (intervalle continu)

### 💎 Batch 3 — Dépasse concurrence (v9.165 → v9.173)
- **v9.173** : Fix audit safe-area + 100dvh + lock autoApply
- **v9.172** : Sentinelle IA auto-check 60s + rapport santé /100
- **v9.171** : FAB flottant contextuel + raccourcis T/P/F/Esc
- **v9.170** : Design futuriste (keyframes modernes, glass, ripple, confetti)
- **v9.169** : Score équité par équipe (balance shifts difficiles)
- **v9.168** : Badges gamification (10 milestones auto)
- **v9.167** : Annotations libres par jour (notes admin globales vPlan)
- **v9.166** : Timeclock + congés structurés auto-apply CP
- **v9.165** : Export CSV planning + iCal admin + helper codeTimeRange

### 🚀 Batch 2 — Extensions + fix P0 (v9.158 → v9.164)
- **v9.164** : Fix XSS emp.id (JSON.stringify) + guards tactiles + lisibilité mobile
- **v9.163** : Recherche rapide employé vPlan
- **v9.162** : Bouton "Aujourd'hui" + print CSS + bandeau conflits
- **v9.161** : Drag vers sélection (bulk apply)
- **v9.160** : Widget "En ligne" vAccueil + badge rôle cadres
- **v9.159** : Heatmap + sparkline cliquables (drill-down)
- **v9.158** : Long-press mobile bulk select + filter chips codes

### 🎯 Batch 1 — UX admin + data viz (v9.153 → v9.157)
- **v9.157** : Fix perf sparkline (cache par render, -90% itérations)
- **v9.156** : Drag & drop cellules admin (swap / Ctrl-copy)
- **v9.155** : Heatmap annuelle + bulk select Shift+Click
- **v9.154** : Live presence avatars + sparkline 12 mois + getRoleIcon
- **v9.153** : Filter chips familles + inline edit double-clic (brosse)

### 📊 Bilan session

- **Notes audits externes** (12 audits, 5 agents × passes) :
  - Avant : 6.62/10 (init)
  - Après v9.193 : 7.92/10
  - Après v9.198 : 8.10/10 (benchmark niche 9.9/10 ⭐)
  - Après v9.202 : 8.50/10
- **3 P0 + 11 P1** détectés, tous corrigés
- **0 régression fonctionnelle** confirmée par audits
- **50 commits, 2 subagents Plan + 15 Explore lancés** (agents parallèles)

### 🏆 Positionnement final

**Leader incontestable niche casino SBM Monaco** : Convention Collective + rotation 40/20 seniors + Article 13 + événements Monaco + multi-casino + bulletin paie pré-visualisé = **aucun concurrent généraliste ne peut reproduire sans 2 ans de R&D juridique monégasque**.

---

## v9.101 → v9.103 — Stabilité + couleurs PDF + NOTES_USER

### v9.103 — Couleurs CODES calibrées sur le PDF SBM original

Screenshots fournis par l'admin (planning avril 2026 v2) ont permis de
remplacer les couleurs inventées par les VRAIES couleurs du PDF :
- Horaires CMC : fonds pastel clairs (rose, jaune, orange pêche, vert tendre, bleu, lavande)
- Variantes CCDP+CMC : orange pêche vif
- Repos/congés : pastel lavande/rose/jaune canari/vert

Cf. `NOTES_USER.md` pour la palette exacte.

### v9.102 — Auto-vérification automatique post-import + 5 outils IA

`doImport()` lance désormais AUTOMATIQUEMENT `_autoVerifyAfterImport(iy, im)` 400ms
après le flush Firebase. Affiche un bandeau visuel (santé + 6 KPIs + warnings détaillés)
dans `#impAutoVerify` + toast résumé.

5 nouveaux outils IA (76 total) :
- `deep_verify_import` : audit profond (couverture, codes inconnus avec positions, orphelins, hors limite, top codes)
- `compare_import_to_reference` : diff A.overrides vs cmc_ref_YYYY-M
- `check_planning_coherence` : cohérence métier (séries > 6j, repos < 8/mois)
- `super_verify_import` : combine les 4 audits en 1 rapport
- (confirmé `verify_import_integrity`)

### v9.101 — Fix CRITIQUE crash Safari iOS + stabilité + lisibilité

**Fix bug bloquant** : `SyntaxError: Invalid escape in identifier: '\'` causé par
3 onclick inline avec `.replace(/'/g,"\\\\'")` mal escapé. Remplacé par :
- Helper `_confirmValidateNewUser(empId)` pour validation nouveaux inscrits (ligne 11291)
- `JSON.stringify(emp.name)` pour setCI (ligne 8919)
- `JSON.stringify(n)` pour createEmpFromImport (ligne 15120)
- Helper `_copyQR()` pour le QR code modal
- Apostrophe courbe `'` dans l'historique de connexion

**Null guards** : `A.user?.id`, EventSource fermé avant recréation, setTimeout .remove() null-safe.

**Lisibilité textes** :
- Variables texte remontées : #e0f0d8 → #eef8e4 (+8%), #a8c8a0 → #c0dcb8
- `text-rendering:optimizeLegibility`, `-webkit-font-smoothing:antialiased`

---

## v9.99 → v9.100 — Audit expert + corrections finales

**v9.99** : Mise à jour documentation (CLAUDE.md, CHANGELOG.md, MEMO_RESUME.md).

**v9.100** : Audit expert via 4 subagents Explore en parallèle, 14 issues identifiées, 7 corrigées.

### P0 critique (sécurité / accès)
- `toggleTVMode()` : ajout guard admin
- `showSyncQueueUI()` : ajout guard admin (exposait clés Firebase)
- `FB_LOCAL` enrichi : `cmc_last_seen_ver`, `cmc_a11y`, `cmc_err_log`, `cmc_theme`, `cmc_last_auto_backup`, `cmc_tts_enabled`, `cmc_lang`
- `fbShouldSync` : exclusions préfixes `cmc_dev_*`, `cmc_import_snap_*`, `cmc_auto_backup_*`
- `vDebug` touch targets : grid auto-fit + min-height 50px (> 44px)

### P1 important
- `admin_set_password` : `hashPwStrong` → `hashPwV2` (sel dynamique)
- `_showImportDiff` : ajout guard admin

### P2 mineur
- Escape universel : ferme tous modals (helpModal, importDiffModal, syncQueueModal, qrModal, releaseNotesModal, cmdPalette, mode TV)
- Undo/Redo : vider stacks lors de `viewAs`/`viewAsBack` (contexte user change)
- `vGestionLive` activité récente : max-height 260px

### Tests
- **54/54 E2E PASS** sur 6 devices (iPhone SE/14 Pro, Galaxy S22, Pixel 7, iPad Air, Desktop HD)
- Aucune régression

---

## v9.71 → v9.98 — Session épique : 28 versions livrées (avril 2026)

**Récap condensé :**

### Sécurité
- **v9.79** Hash v2 sel dynamique par user + device fingerprint anomaly detection
- **v9.81** Backup auto quotidien (7j rotation) + Firebase Security Rules template
- **v9.87** Générateur mots de passe sécurisés (14 car., strength meter)
- **v9.92** Proxy Anthropic template (Cloudflare Workers gratuit)

### Import fidélité P0+++
- **v9.74** Preview/Diff avant commit + Snapshot/Rollback + Signature SHA-256
- **v9.78** Matching Levenshtein + Metaphone + validation sommes

### IA surpuissante
- **v9.73** 50 outils (19 admin)
- **v9.77** 63 outils + mode proactif
- **v9.87** 71 outils au total + password generator + CSV export + audit report
- **v9.90** Réactions emojis chat

### UX / Productivité
- **v9.75** Système d'aide contextuel `?` + 15 sujets
- **v9.82** Command palette ⌘K (42 actions) + shortcuts clavier (? 1-9)
- **v9.83** Undo/Redo global ⌘Z / ⌘⇧Z (50 actions)
- **v9.84** Dashboard LIVE + Mode TV grand écran (Wake Lock)
- **v9.85** Suggestions proactives + Dark/Light/Auto theme
- **v9.97** Release notes in-app + auto-affichage nouvelle version

### Performance
- **v9.86** IndexedDB wrapper + lazy-loader + throttle helper
- **v9.89** Error monitoring local + Performance tracking (Core Web Vitals)

### Accessibilité AAA
- **v9.94** Skip links + ARIA + High contrast + Font scaler + Reduce motion

### Resilience
- **v9.95** Retry jitter exponentiel + UI visible file sync
- **v9.96** Circuit breaker Firebase (5 échecs = 60s cooldown)

### PWA avancée
- **v9.93** Badge API + Web Share + Wake Lock + Shortcuts long-press

### Qualité / CI
- **v9.91** Page Debug admin (perf + erreurs + santé) + QR code generator
- **v9.98** Tests E2E Playwright 54 tests × 6 devices + CI/CD GitHub Actions

### Fixes / Polish
- **v9.71** EmailJS retiré + validation nouveaux + fix alignement planning
- **v9.72** STT (dictée) réparée + TTS documenté
- **v9.76** 8 sujets d'aide supplémentaires
- **v9.80** Horaires multi-rôles (inspecteur/sup/pitboss) préparation
- **v9.88** UX helpers (emptyState, skeleton, confirmDanger, statusBadge)

### Tests finaux v9.98
- **54/54 tests E2E PASS** (6 devices × 9 checks)
- 0 erreur runtime
- 27+ features critiques validées sur iPhone SE, iPhone 14 Pro, Galaxy S22, Pixel 7, iPad Air, Desktop HD

---

## v9.70 — Fixes responsive mobile complets (iPhone SE / Android)

**Tests multi-devices Puppeteer** : 5 profils (iPhone SE, iPhone 14 Pro, Samsung Galaxy S22, Pixel 7, iPad Air) avec viewports, userAgent et touch émulés.

### Avant fixes
- 55 PASS · 0 FAIL · **25 WARN** (overflow horizontal sur accueil/chat/IA/monplanning)
- Nav bas débordait avec 8 onglets admin (585px sur viewport 375px)

### Fixes appliqués

#### 1. Nav bas `#bnav` (ligne 285)
- Ajout `overflow-x:auto;overflow-y:hidden;scrollbar-width:none` (scroll interne)
- `.nt` : `flex:1 1 auto` (shrink autorisé), `min-width:36px`, `white-space:nowrap;overflow:hidden`
- Media query `@media (max-width:419px)` : labels cachés (ne garde que les emojis), padding réduit, `min-width:34px`
- Template `vNav` : wrap du label dans `<span class="nt-lb">` pour permettre le `display:none`

#### 2. Raccourcis accueil (ligne 6854)
- Ajout `max-width:100vw;overflow-y:hidden;scrollbar-width:none` sur le scroll container
- Child : `width:max-content` explicite pour bien contenir les boutons

#### 3. Toolbar vIA (ligne 14448)
- Ajout `flex-wrap:wrap` → les 5 boutons admin wrapent sur 2 lignes si nécessaire

#### 4. Chat header (ligne 8390)
- Ajout `flex-wrap:wrap;justify-content:flex-end` → 🗑 Vider + 🔍 wrapent

#### 5. MonPlanning header (ligne 5900)
- Ajout `flex-wrap:wrap` → 📄 PDF wrape si overflow

#### 6. Safety net global (ligne 75-76)
- `html{overflow-x:hidden}` + `body{overflow-x:hidden;max-width:100vw}` → empêche tout scroll horizontal au niveau page entière

### Résultat

| Device | Avant | Après |
|--------|-------|-------|
| iPhone SE 375×667 | 10/16 | **14/16** ✅ |
| iPhone 14 Pro 393×852 | 11/16 | **14/16** ✅ |
| Galaxy S22 360×780 | 10/16 | **14/16** ✅ |
| Pixel 7 412×915 | 11/16 | **14/16** ✅ |
| iPad Air 820×1180 | 13/16 | **14/16** ✅ |

**Total : 70 PASS · 0 FAIL · 10 WARN** (les 10 warn restants sont "touch targets < 32px" sur éléments décoratifs non interactifs)

Le warning `Nav 0 onglets (min Infinity)` était un faux positif du test (nav rendu dans le template inline).

---

## v9.69 — Audit expert + corrections post-audit

**Audit parallèle avec 4 subagents Explore** (sécurité, logique métier, UI/CSS/mobile, feature MOTD).

### Fixes P1 (importants)
- **`fbApplyData`** : `cmc_motd` maintenant géré explicitement
  - Accepte `null` (effacement depuis un autre appareil) → set `A.motd=null` + localStorage + dcDebounced
  - Validation type : refuse tout sauf `null` ou `{text,ts,author}` (protection anti-corruption)
  - Avant : effacement distant silencieusement ignoré (bug sync multi-appareils)

### Fixes P2 (mineurs UX)
- **vAdmin MOTD** : auteur supprimé affiche `par U11804 (supprimé)` au lieu de `par undefined`
- **vAccueil bandeau MOTD** : ajout `word-break:break-word` + `overflow-wrap:anywhere` pour empêcher débordement horizontal sur textes longs sans espaces (iPhone SE 375px)

### Méta
- **CLAUDE.md** : nouvelle section **"🧰 Outils & réflexes expert"** en tête de fichier — boîte à outils personnelle pour futures sessions (outils par besoin, commandes de validation, pièges connus, stratégies qui marchent)

### Non corrigés (après analyse)
- `table-layout:fixed` dans vPlan (ligne 6802) : faux positif — la table a bien une `width:...px` explicite, comportement correct
- `width` sur vDeparts (ligne 7143) : `min-width:100%` est intentionnel pour sticky headers
- Toast `c.icon` sans esc : emoji hardcodé dans `conf` (pas de user input), risque théorique uniquement
- Roulette chef forcé senior (ligne 12416) : règle métier existante, ne pas modifier sans validation utilisateur

---

## v9.68 — Message du jour admin + sync Firebase

- **Nouveau store** `A.motd` : `{text, ts, author}` ou `null`
- **FB_FIX** : `cmc_motd` ajouté (sync Firebase multi-appareils)
- **Fonctions admin** (guard `AID`) :
  - `setMotd(text)` — publie/met à jour (max 500 car., audit `motd_set`/`motd_clear`)
  - `clearMotd()` — efface
  - `adminSetMotdFromInput()` — lit `#motdIn` dans vAdmin
- **UI admin** (`vAdmin`) : bloc doré avec textarea 500 car. + boutons Publier / Effacer, affiche date+auteur si publié
- **UI employé** (`vAccueil`) : bandeau doré 📢 visible en haut pour tous les connectés, avec pre-wrap (sauts de ligne conservés), date/heure
- `esc()` appliqué sur le texte (XSS-safe), sync temps réel via `fbWrite` → `fbApplyData`

---

---

## Historique versions (v8.83 → v9.32)

| Version | Changements |
|---------|-------------|
| v8.83 | Chat DMs, réponses, suppression admin, filtres, séparateurs date |
| v8.84 | adjDeparts avec getBoundingClientRect, reset _chatDm à navigation |
| v8.85 | Workflow Claude Code (.claude/settings.json) |
| v8.86 | Import niveau 4.5 prénom A.reg, guards bigram homonymes |
| v8.87 | Éditeur identité admin (prénom/nom/email), recherche universelle étendue |
| v8.88 | Banque données évolutive : détection nouveaux/départs à l'import |
| v8.89 | vMonPlanning (planning personnel), récap import par catégorie |
| v8.90 | Journal sécurité admin, logAdminSession, vAdminSecurity |
| v8.91 | Présence temps réel (vOnline), historique connexions, horloge topbar, CSS animations |
| v8.92 | adjGrid scroll vers aujourd'hui (vPlan), tri équipes croissant, data-planday |
| v8.93 | Fix couleur rgba vMonPlanning, CLAUDE.md ToolSearch |
| v8.94 | Bouton admin désactiver/activer IA Claude |
| v8.95 | Recherche internet (web_search), mode local toujours actif |
| v8.96 | Login par nom+prénom (matricule optionnel), adminChangeEmpId, adminResetAllPw |
| v8.97 | findEmpByName() — recherche prénom complet, préfixe STOCKÉ = préfixe SAISI |
| v8.98 | Firebase Realtime Database (sync temps réel tous appareils), FB_DEFAULT hardcodé |
| v8.99 | Fix step1 matricule ignoré, nettoyage DEF_EMP doublons, vMonPlanning message sync |
| v9.0 | viewAs/viewAsBack (vue-employé admin), reset chat, reset compte complet (pw+reg) |
| v9.1 | Fix recherche : searchInput() helper, inputs empQIn/pwQIn ne perdent plus le focus |
| v9.2 | Admin voit son équipe+miroir en tête dans vPlan et vDeparts, sticky solides |
| v9.3 | Section headers sticky vPlan, overflow-x:clip body, gm() fallback DEF_TEAMS, requestAnimationFrame scroll |
| v9.4 | Zoom activé (max-scale=5), vDeparts tous les jours du mois, logUserLogin enrichi (IP/UA/écran/timezone), logIAInteraction (cmc_ia_log Firebase), adminSetPw, fiches employés nom/prénom/🔒 |
| v9.5 | Audit sécurité : XSS corrigés (3 handlers d'erreur), guard doResetPwDirect, hashPwStrong() 10k rounds backward-compat, proxy IA (iaSetProxy/cmc_ia_proxy), cmc_admin_pin dans FB_LOCAL, fallback base=0 cohérent vDeparts↔calcDepPos, adjMonPlanning scroll aujourd'hui, colonne nom 100-130px |
| v9.6 | Fix noms collés aux horaires, adjDeparts scroll indépendant par grille, col noms 72px |
| v9.7 | Import avec confirmation si corrections manuelles, touchSession dans heartbeat, fbWrite retry + syncQueue, améliorations vMonPlanning/vAccueil/topbar, compteur employés vPlan |
| v9.8 | fbApplyData clone profond, tc() validation hex couleur, retraités détectés à l'import, title noms complets, toast queue anti-collision, line-height planning |
| v9.9 | Queue offline (sync auto retour online, badge ⏳), notifications navigateur (planning modifié, nouveaux messages), échanges de shifts (demande depuis vMonPlanning, vue admin RH/refus/swap, audit complet, sync Firebase) |
| v9.10 | Fiche de renseignement (onglet Profil 👤) : employé remplit adresse/naissance/USBM/poste, batch Firebase unique, whitelist champs sécurisée. ROTATION 55+ corrigé : mêmes patterns standard, max 40min par défaut / 60min avec accord. Admin voit la fiche dans vEmps. |
| v9.11 | Audit complet + corrections : compétences import BRTPECK (toutes familles), rotation départs base=ei, A.user/_viewAs refresh post-SSE, vPlan table width:auto colonnes 62/28px, chat utilisateurs en ligne (DM rapide), notifs iOS guide PWA, nav admin 8 onglets (Stats→Admin panel), _checkNewChat toast si visible hors-chat, PWA meta (title+theme-color), SW cache v9.11 |
| v9.12 | Fix ReferenceError myPl dans vAccueil (→ variable code), fix largeur colonne noms vPlan (wrapper div .nw 90px — max-width ignoré par browsers sur td en auto-layout) |
| v9.13 | Fix chat DM reply : chatSetReply auto-active _chatDm → réponse privée bien routée. Nettoyage code mort (chatSetDmEmp, commentaires genBase obsolètes) |
| v9.14 | Workflow expert CLAUDE.md, fix crash .touches[0], guards admin (removePinCode, savePinCode, auditDelEmp, auditRenameEmp), XSS picker, PIN hashPwStrong, strip .clear Firebase, font-size 11px min, touch targets 44px, import _importSkipped |
| v9.15 | Audit complet + corrections 10 fixes, refactoring vPlan (table-layout:fixed + colgroup puis revert auto), initAxisLock factorisé, bloc infos connexion admin |
| v9.16 | Refonte chat (bulles gradient, avatars, SVG Casino en fond), suppression module échanges, Casino Live placeholder, anniversaires, fêtes dynamiques (Pâques calculé), logo SBM SVG |
| v9.17 | Améliorations UX : cartes premium, raccourcis carousel horizontal accueil, badges statut employé, login progression |
| v9.18 | Statistiques annuelles avec archive, ROLES (12 postes hiérarchiques), Chefs Cartes (label famille) |
| v9.19 | Stats complètes : calcStats (mois/année/carrière) + heures, vMesStats employé, vStatsAnnuelles admin, CODE_HOURS override 16/3=9h coupure |
| v9.20 | vStatsGlobal (effectifs par lieu/rôle/famille), VENUES (6 lieux), archivage par employé |
| v9.21 | Fix doublons inscription reg_alerts, import avec fusion (plus de table rase), synthèse vImportVerif |
| v9.22 | teamForMonth (teamHistory par mois), stats fiche admin, bloc stats accueil, mode visiteur SBM |
| v9.23 | OTP vérification email (genVerifCode/checkVerifCode/adminValidateCode, cmc_verif_codes FB_FIX), EmailJS optionnel, connexion multi-critères (email/mat/nom+prénom) |
| v9.24 | Audit complet (4 agents) + 4 corrections UX P0/P1 (font-size, touch targets 44px, axis-lock seuil 10px, touch-action:pan-x pan-y) |
| v9.25 | 7 améliorations expert : backup/restore JSON, IA prompts enrichis, réactions emoji chat, login autocomplete datalist, badge sync Firebase, runTests framework (16 tests unitaires) |
| v9.26 | Bulk actions admin : mode sélection + 6 actions groupées (équipe, rôle, lieu, reset MDP, export CSV, supprimer) avec confirmation double |
| v9.27 | Fix bug doublon PORTA (dédup avec migration données), fix nav PWA iOS (viewport-fit=cover, safe-area-inset, translateZ(0), touch-action), outil detectDuplicates |
| v9.28 | Compétences corrigées (P=Punto Banco, P+=Punto HR, K=Poker Cash Game), dateEntreeSbm + dateEntreeJeux (admin RH + affichage carrière profil) |
| v9.29 | Convention collective SBM intégrée : CONVENTION (29 articles), BULLETIN_CODES, grilles salaires, vConvention (4 onglets), prompt IA enrichi |
| v9.30 | Fix calcDepPos désynchro avec vDeparts (bug PORTA étape 2), USBM→USM label, ROLES enrichi (chef américain au lieu de chef cartes), fusion vEmps/vPasswords (bouton raccourci), XSS setCI, contraste WCAG .sec-sub |
| v9.31 | Note DRH 2021 congés familiaux : extension PACS/union libre, règles d'application (prise obligatoire, R/RH, décompte ouvrés, si absence), UI enrichi vConvention, prompt IA mis à jour |
| v9.32 | Règles des 8 jeux de table (JEUX), onglet Jeux dans vConvention (accordéon détails), graphique SVG heures/mois dans stats (renderStatsCard), recherche chat avec barre toggle, animation msgIn (fade+translate nouveaux messages) |
| v9.33 | Audit post-v9.32 : JEUX injecté dans buildIASystemPrompt, fix layout chat header (double margin-left:auto), suppression helpers morts getJeu/getJeuByComp |
| v9.34 | Upload photos perso (chatBg/accueilBg/loginBg/planningBg) via file input admin + base64 resize 800x800, banque documents multi-format (vDocs : photos/PDF/DOC/XLS, 6 catégories, preview lightbox, filtres), fix topbar safe-area-inset-top iOS notch, fix vChat height 100dvh |
| v9.35 | Helper empAvatarHtml (img si photo sinon lettre), upload photo self-service (vMonProfil), upload photo admin (vEmps édition), avatars photo partout (topbar/chat/accueil/profil/emps), planningBg appliqué vPlan/vDeparts via classes body.has-*-bg |
| v9.36 | Audit v9.35 + 3 corrections : vIA height 100vh→100dvh, planning bg sans ::after (direct sur .bg-planning via background-attachment:fixed), data URLs quotées défense XSS (empAvatar/chatBg/vDocs/vMonProfil/vEmps/CSS vars) |
| v9.37 | **Glass morphism total + dossier partagé** : suppression silhouettes SVG par défaut (.bg-casino::before / .bg-palace::before), photos user appliquées sur body via CSS vars (--cmc-accueil-bg, --cmc-planning-bg actif en vPlan/vDeparts), cards/topbar/nav semi-transparents avec backdrop-filter:blur, encadrés vPlan/vDeparts/vPartage/vDocs transparents, family headers enrichis avec icônes (🃏♦️♠️) et gradient, bulles chat plus translucides, **chat carousel multi-images** (chatBg + chatBg2 + chatBg3 avec animation CSS 30s), **champ `shared` sur docs** + `toggleDocShared` + `uploadDocShared`, vDocs avec 3 onglets (Tous/Privés/Partagés) + boutons upload séparés (privé vs partagé), **nouvelle vue vPartage** accessible à TOUS les employés (read-only, docs shared seulement), helper _renderDocCard réutilisé, catégories docs enrichies (+Procédures +Plans du Casino = 8 dossiers), quick link "👥 Dossier partagé" dans accueil |
| v9.38 | Audit post-v9.37 : 3 fixes XSS défensifs sur data URLs dans CSS/attributs. Nouveau helper `_safeUrl()` qui encode `"`, `\`, `'`, `(`, `)` en `%XX`. Appliqué à 10 endroits : empAvatarHtml, vDocs thumbnails, previewDoc img/iframe src, render() CSS vars (loginBg/accueilBg/planningBg), vMonProfil photo 80x80, vEmps édition 64x64, vAdmin photos 52x52, chat-bg-layer carousel. Fix clignotement chat avec 1 seule photo (classe `.multi` requise pour l'animation). |
| v9.39 | **5 FIX CRITIQUES** + améliorations visuelles. (1) **BUG fonds d'écran invisibles** : `<div id="app" style="background:#1a3020">` inline masquait le body → changé en `transparent`. (2) **FAMILIES renommées** : roulettes→"Jeux européen" / bj→"Jeux américains" / nouveau **baccara**→"Baccara" / cmc→"Groupe ouvert" + mise à jour de tous les hardcodes (FAMS_ORDER, FAM_ICONS, FAM_LABELS, FAMS vPasswords, audit reports, IA prompt, stats global). (3) **calcStats robuste** : normalisation code (`String().trim().toUpperCase().replace(/[*']/g,"")`), nouvelle catégorie `st.autre` pour codes inconnus, support codes SBM (FL, AT, MT, ABS, etc.). (4) **vPasswords affiche mdp en clair** si stocké dans `clear` : bloc inline avec reveal 👁️, copier 📋, et input nouveau + bouton "Définir". (5) **Limites upload** : DOC_MAX_SIZE 5→25 MB, DOC_TOTAL_WARN 30→200 MB, photos 5-8→15 MB, resize app 800→1600, resize docs 1200→2000. **Améliorations visuelles** : opacity body overlay réduite (.55/.62 vs .82/.88) pour voir vraiment la photo, nouvelles animations (`pageIn`, `cardPop`, `pulseDot`, `floatUp`), hover lift sur `.card` (+translateY -2px), bouton gold gradient animé, scrollbars discrètes. |
| v9.40 | Hot-patch audit v9.39 — 2 bugs logiques mineurs corrigés. (1) `calcStats` ligne 4673 : retiré "CP" du test `base==="FL"\|\|base==="CP"\|\|...` (déjà traité plus haut, jamais atteint pour CP brut, code confus). (2) PDF import étoile → senior : ne force plus `family="roulettes"` ni `chef=true` (ligne 7424). Avant, toute étoile dans le PDF forçait l'employé en famille roulettes + chef, ce qui est incompatible avec la nouvelle famille Baccara. Maintenant l'étoile marque uniquement `senior=true` et laisse family/chef intacts. |
| v9.41 | **Compression automatique des images** au téléchargement. `_resizeImage(file,maxW,maxH,quality,cb,budgetBytes)` devient progressive : 6 tentatives en cascade (100% → 85% → 70% → 55% → 40% → 30% de maxW, qualité .85→.60) jusqu'à rentrer dans le budget. Si canvas.toDataURL throw (iOS OOM), fallback automatique vers tentative plus petite. Callback reçoit maintenant 3e argument `info={w,h,q,size}` pour logging. Budgets appliqués : photos app 800 KB, avatars employé 60 KB (300×300), docs images 90% de DOC_MAX_SIZE (22.5 MB effectif). `uploadDoc` étendu : accepte aussi .ppt/.pptx/.zip, messages d'erreur non-images avec taille réelle et limite, compteur `okCount` pour upload multi-fichiers résilient. Limite fichier brut remontée à 50 MB (la compression fera le reste pour les images). |
| v9.42 | **Groupe contractuel SBM (ouvert/fermé) + fix race condition viewAs**. (1) Nouvelle constante `GROUPES` : "ouvert" (polyvalent, mobilité jeux, prime 150€) / "ferme" (noyau historique pré-1993, retraite étendue, prime 280€). Champ `emp.groupe` optionnel sur chaque employé. (2) Picker dans vEmps édition avec description. (3) Badge visuel 🔓/🔒 dans la liste employés. (4) Affichage dans vMonProfil section Carrière avec explication. (5) Prompt IA enrichi avec statuts contractuels et règles métier (un groupe ouvert peut tourner sur plusieurs familles, baccara ne peut pas travailler aux roulettes européennes). (6) **Fix race condition `fbApplyData`** : en mode viewAs, si l'employé visualisé est supprimé pendant sync Firebase, retour automatique au compte admin avec toast d'alerte. |
| v9.43 | **Badges visuels senior + groupe fermé** — uniformisés dans toute l'app. (1) `empLabelHtml` : ★ doré avec text-shadow glow (#ffce3a) pour senior 55+, badge "F" marron (🔒 background) pour groupe fermé. Les deux badges apparaissent automatiquement partout où cette fonction est appelée (planning, départs, chat, listes). (2) `empAvatarHtml` enrichi : pastille étoile dorée dégradée en coin haut-droit (senior) + pastille verrou marron en coin bas-droit (groupe fermé) + bordure spéciale dorée/marron autour de l'avatar + glow box-shadow assorti. Le wrapper relatif n'est créé que si au moins un badge est présent (compat flex layout). (3) Checkbox "55+ ans" dans vEmps édition passe du rouge au doré. (4) Liste vEmps : badge F (🔒) ajouté à côté du badge senior. |
| v9.44 | **Topbar compact + chat plus lisible**. (1) **Topbar refactoré** : titre "CMC Teams v9.44" sur une ligne avec ellipsis, sous-titre "Casino de Monaco" plus petit (11px au lieu de 12px, letter-spacing 1.5px au lieu de 3px), badge ADM compact (3 lettres), nom/ID user retirés (avatar suffit), bouton sync condensé en dot 10px avec tooltip. Tout tient maintenant sur une ligne même sur iPhone SE 375px. (2) **Bulles chat éclaircies** : `.msg-me` passe à gradient doré vif (rgba 220,170,40,.85 → 180,130,20,.78) avec texte sombre #0a1408 pour contraste max. `.msg-ot` devient blanc laiteux (rgba 245,250,240,.96 → 225,238,220,.93) avec texte sombre #0a1a0c. `.msg-dm` violet éclairci (rgba 170,130,230,.88) avec texte blanc. Lisibilité parfaite sur tout fond de photo. (3) **Règles CSS adaptatives** : `.msg-ot .msg-ts`, `.msg-ot .msg-name`, `.msg-ot .msg-reply-q`, `.msg-ot .chat-act-btn` forcés en couleurs sombres sur bulles claires. (4) **Filtres chat visibles** : boutons Tous/Publics/Privés passent de color #4a6840 (invisible) à #d8e8c8 + background rgba(0,0,0,.35) + border rgba(201,162,39,.3) + backdrop-filter blur. État actif en #ffdc40. Bouton Vider 🗑 rouge visible. Bouton recherche 🔍 avec background + bordure. (5) **Header Chat Casino** : couleur passe de #c9a227 à #ffdc40 + text-shadow. "X messages • Casino de Monte-Carlo" passe de rgba(.25) à rgba(.75) + text-shadow. (6) **Chat datesep** : "— Aujourd'hui —" encapsulé dans pilule background rgba(0,0,0,.35) + blur pour lisibilité sur fond photo. |
| v9.45 | **Session expert 3 rôles** — ajouts massifs en une session. **Rôle Planning** : (1) `duplicateMonthFromPrev()` duplique le planning du mois M-1 vers le mois courant avec confirmation. (2) `detectRepoConflicts(y,m)` détecte 3 types d'alertes (repos insuffisant, 7+ jours consécutifs, données manquantes) avec 3 niveaux (critical/high/info). (3) `vRepoConflicts()` nouvelle vue admin groupée par sévérité avec bouton "Voir" vers fiche employé. (4) **Dashboard RH** dans vAccueil admin : 6 KPIs jour courant (en service, repos, congés, malades, formation, actifs) + bandeau alerte conflits cliquable. Helper `_hrKpi()`. (5) `calcStats` enrichi avec `nuits`, `coupures`, `weekEnds` pour équilibrage shifts difficiles. (6) `renderStatsCard` affiche nouveau bloc "🌙 Shifts difficiles". (7) 2 nouvelles entrées admin items + route `conflits` dans vMain. **Rôle Designer** : (1) `:root` tokens CSS complets (palette Monaco, --sp-0 à --sp-10 spacing, --r-sm à --r-full radius, --sh shadows, --t timings, --fs typography scale). (2) Classes utility `.h1/.h2/.h3/.h4`, `.txt-sm/.txt-xs/.txt-mono/.txt-upper`. (3) Badge system `.badge` + variantes gold/green/red/blue/violet/muted. (4) Empty states `.empty/.empty-icon/.empty-title/.empty-sub/.empty-cta`. (5) Skeleton loading `.skel/.skel-line/.skel-circle` avec animation shimmer. **Rôle IT (sécurité)** : (1) **Meta CSP restrictive** (default-src self, script-src +cdnjs, img-src data:, connect-src firebase/anthropic/emailjs, object-src none, base-uri self, form-action none). (2) PDF.js `crossorigin="anonymous"` + `referrerpolicy="no-referrer"`. (3) Metas complémentaires : color-scheme dark, format-detection, robots noindex, referrer no-referrer, description. (4) **Error boundary renforcé** : fonction `_showErr` centralisée avec `_escHtml`, compteur `_errCount` max 3 avant reload auto, log dans `cmc_last_err` (5 derniers), bouton "Reset complet" qui vide localStorage, `role="alert"` + `aria-live="assertive"`, `<details>` pour stack trace, aria-labels sur boutons. (5) **`unhandledrejection` handler** global pour catch Promise rejections non gérées. |
| v9.46 | Hot-patch audit v9.45 — 3 mineurs + 1 UX corrigés. (1) **Cache memoization `detectRepoConflicts`** : évite ~23K ops/render en cachant le résultat par clé `(y,m,emp_count,JSON_signature_length)`. La signature JSON change à toute modification des overrides → auto-invalidation sans tracking explicite. (2) **`gpl(y,m)` accepte params optionnels** : refactor propre au lieu de l'IIFE qui mutait temporairement `A.year`/`A.month` (race condition potentielle). Backward compat : `gpl()` sans args utilise toujours A.year/A.month. (3) **`vRepoConflicts` guard** : si non-admin → `sv("accueil")` au lieu de retourner page vide silencieuse. (4) **Vérifié** : pas de conflit CSS avec les nouvelles classes `.h1/.h2/.h3/.h4` — aucun élément existant ne les utilisait. Audit passe à **SAFE_TO_RELEASE clean**. |
| v9.47 | **Pas à pas batch 1 — 3 outils planning expert**. (1) **`renderEmpHistory(empId)`** : nouveau bloc "📜 Historique des modifications" dans la fiche employé édition admin. Affiche les 30 dernières entrées de `cmc_audit` filtrées par `eid`, avec labels humains (emp_create→"➕ Créé", pw_set→"🔐 MDP", etc.), date format FR, old→new change visible. Scroll interne max-height 240px. (2) **`exportPlanningCSV` enrichi pour la paie** : passe de 6 colonnes + jours à **20 colonnes + jours** — Matricule / Nom / Équipe / Famille / Groupe contractuel / Rôle / Jours / Jours_Travail / Heures_Total / Jours_Repos / RH / R / CP / M / AF / RRT / HC / CDP / Nuits / Coupures / Week_Ends / Autre. Toast indique le nombre de colonnes. Échappement CSV-safe (wrap + guillemets doublés). Audit log ajouté. Nom de fichier : `planning-paie-{mois}-{année}.csv`. (3) **`vQuiEstLibre()`** : nouvelle vue admin "🔵 Qui est libre ?" avec sélecteur de jour (mini-calendrier cliquable + boutons ◀ ▶), 6 KPIs (en service/repos/congés/malades/formation/non défini), liste détaillée des employés hors service groupée par catégorie avec avatar + équipe + code, détails collapsible des employés en service, empty state si tout le monde bosse. Route `quilibre` dans vMain. Entrée admin "😴 Qui est libre ? (par jour)". |
| v9.48 | **Pas à pas batch 2 — Designer refinements & accessibilité**. (1) **Focus-visible accessibilité** : outline or (#ffdc40) avec glow uniquement au clavier (pas au clic souris) — respecte `:focus-visible` standard. Inputs/selects/textareas avec outline + box-shadow assortis. (2) **Skip link** `.skip-link` caché par défaut, visible au premier tab pour navigation clavier. (3) **`prefers-reduced-motion`** : désactive toutes les animations/transitions si l'utilisateur a activé le mode réduction mouvements (iOS/macOS accessibility). Inclut le carousel chat. (4) **`.sr-only`** classe utility pour labels screen reader uniquement (WCAG). (5) **Transitions fluides entre vues** : `#content` a maintenant une animation `contentFade` de 0.28s à chaque changement de vue (opacity + translateY). (6) **Tablet breakpoint 768px** : contenu centré avec `max-width:900px`, padding `.page` augmenté à 24px 28px, nav centrée. (7) **Desktop breakpoint 1024px** : max-width 1100px. (8) **Safe-area iOS renforcé** : `#content` a maintenant `padding-left/right:max(0,env(safe-area-inset-left/right))` via `@supports`. |
| v9.49 | **Pas à pas batch 3 — Lisibilité priorité + IT sécurité/perf**. Demande utilisateur : "Malgré que je veuille de la transparence pour les photos la priorité est aux informations. Bien visible." (1) **Cards opacité renforcée** : `.card` passe de rgba(.55,.62) à rgba(.82,.88) + backdrop-filter blur(14px) + shadow plus fort. Lecture des infos largement prioritaire sur la photo de fond qui reste visible par transparence. (2) **Topbar** : bg rgba(.85)→(.92), border or .35→.45. **Nav bas** : idem (.88→.94). (3) **Sec-title & sec-sub** : ajout text-shadow 0 1px 3px rgba(0,0,0,.6) pour lisibilité max sur fond photo. (4) **Tables planning/départs** : headers `.sth/.dth/.stth` passent de bg #223a26 opaque à rgba(18,32,22,.96) avec text-shadow. Border or passe de .3 à .4. Noms employés (`.ntd`) en rgba(16,30,20,.95). (5) **Aria-label** sur bouton logout topbar. (6) **Lazy-load PDF.js** : nouveau `loadPdfJs()` qui charge le script cdnjs UNIQUEMENT quand l'admin importe un PDF (avant, 500 KB chargés au démarrage pour tous). Gain perf chargement initial ~500 KB. `_extractPdfLines` refactorisé pour appeler `loadPdfJs().then(_doExtractPdfLines)`. Message toast "⏳ Chargement de PDF.js…". |
| v9.50-v9.57 | Batchs intermédiaires : département Jeux de table + fonds perso par employé (v9.50), diaporama fond d'écran (v9.51), fix cadena/œil vEmps + BACCARA import (v9.52), Phase 2 experts templates/solde CP/retardataires (v9.53), calendrier d'affluence Casino (v9.54), export ICS par équipe + toast ARIA (v9.55), hot-patch audit import BACCARA regex + apostrophes (v9.56), hot-patch XSS vAuditLog + guard calcLeaveBalance (v9.57). Voir git log pour le détail complet des commits. |
| **v9.58** | **Accès par employé + hot-fix scintillement import**. (1) **Store `A.access`** `{uid: {mode, blocked}}` sync Firebase via `cmc_access` (FB_FIX). Modes : `auto` (détection automatique) / `general` (force vue générale) / `personal` (force vue perso). Features blocables : `monPlanning`, `echanges`, `ics`. (2) **Helpers** : `getUserAccess()`, `isUserFloating(u,y,m)` (détecte user flottant via : override admin → team invalide/absente → u.visitor → pas dans le planning importé), `isFeatureBlocked(uid,feat)`, `setUserAccess(uid,mode,blocked)` (guard AID + audit log `access_change`). (3) **Fix critique `vDeparts`** : pour un non-admin avec `team="?"`, `show` était vide → page blanche. Désormais `show = (isAdm \|\| _isFloating) ? _depPri.concat(_depRest) : _depPri.slice()` + bandeau "Vue générale". (4) **Adaptations vues** : `vMonPlanning` (carte bienvenue + boutons vers vue générale si flottant, message "Accès restreint" si `monPlanning` bloqué, masquage bouton ICS si bloqué), `vAccueil` (masque "Mon Service" si flottant — utilise `_isFloating` au lieu de `u.visitor\|\|u.team==="?"`), `vPlan` (bandeau "Vue générale" + `utid=null` si team invalide). (5) **Modale admin `🔐 Accès`** : `renderAccessModal()` (radio modes + checkboxes features) injecté via `accessModalWrap` dans `render()`/`dc()`. Bouton dans `vPasswords` + badge 🔐 sur cartes avec accès personnalisé. (6) **`dcDebounced()` via requestAnimationFrame** : batch les `dc()` en rafale lors du SSE Firebase (fin du scintillement CSS `fadeIn` qui se rejouait à chaque update). Utilisé dans `fbApplyData` et storage listener. (7) **`_importInProgress` + `_importPending` + `_flushImportPending()`** : pendant `doImport()`, les `fbWrite()` sont bufferisés (1 entrée par clé, dernière valeur gagnante) et flushés une seule fois en fin d'import. Élimine les ~200 allers-retours SSE pendant l'import + les écritures concurrentes qui déstabilisaient le parsing. |
| **v9.59** | **Fix fond perso cas par cas + flèche retour globale + ajout Three Card Poker**. (1) **Bug fond perso** : `_collectBgPhotos()` ajoutait le fond perso employé ET les fonds globaux → diaporama mélangé. Fix : si l'employé a un `customBg` dans `A.reg[uid].customBg`, `return photos` immédiat → l'employé voit UNIQUEMENT son fond, pas de rotation avec les globaux. (2) **Flèche retour globale** : nouveau stack `_viewHistory` + fonction `goBack()`. `sv(v)` push la vue courante dans l'historique avant de changer (sauf si `_goingBack=true`). Flèche ← ajoutée dans `vTopbar()` à gauche du titre, visible seulement si `A.view!=="accueil"` et `_viewHistory.length>0`. Permet de quitter n'importe quelle page vers la précédente, plus besoin de forcer retour admin. Limite à 30 entrées pour éviter leak mémoire. (3) **Three Card Poker** ajouté à `JEUX.tcp` avec code compétence `3`, catégorie poker, règles résumées + mises Ante/Play/Pair Plus + paiements Ante Bonus et Pair Plus. |
| **v9.60** | **Partage par dossier + fix mdp clair perdu au sync + logs connexion enrichis**. (1) **Partage par catégorie** : nouveau store `A.docCatShared = {catId: bool}` sync Firebase via `cmc_doc_cats_shared` (FB_FIX). Helper `isDocShared(d)` = `!!d.shared \|\| !!A.docCatShared[d.cat]`. `toggleDocCatShared(catId)` avec guard AID + audit log `doc_cat_shared_toggle`. Nouveau panneau vDocs "Partage par dossier" avec un bouton toggle 🔒/👥 par catégorie (8 catégories : photos, règlements, procédures, contrats, notes, formations, plans, autres). vPartage inclut maintenant TOUS les docs des catégories partagées (pas seulement ceux avec `shared: true`). (2) **Fix mdp en clair perdu au sync Firebase** : `fbWrite("cmc_pw", ...)` strip le champ `clear` avant envoi Firebase (bien pour sécu), mais `fbApplyData("cmc_pw", vc)` faisait `A.passwords=vc` → écrasait le `clear` local après chaque resync → vPasswords n'affichait plus les mdp. Fix : merge dans fbApplyData qui préserve `clear` local si existant et absent dans la version Firebase. (3) **Logs connexion enrichis v9.60** : `logUserLogin` détecte maintenant **browser** (Chrome/Edge/Firefox/Safari/Opera) et **OS** (iOS/Android/Windows/macOS/Linux) depuis UA, et récupère **géolocalisation** via `https://ipwho.is/` (HTTPS gratuit sans clé) — ajoute city, region, country, isp. Fallback sur `api.ipify.org` si ipwho.is échoue. CSP `connect-src` étendue avec `https://ipwho.is` et `https://api.ipify.org`. Affichage vOnline enrichi avec 📍 lieu, 📡 ISP, 💻 navigateur+OS, 🕐 timezone. |
| **v9.61** | **Hot-fix audit indépendant v9.58→v9.60**. Subagent Explore audit a trouvé 2 P0 + 1 P2 : (1) **P0 critique `fbApplyData("cmc_pw")` merge `clear` incorrect** : si admin change mdp via un autre appareil, Firebase envoie `{h:nouveau}` sans `clear`, le merge préservait l'ancien `clear` local → vPasswords affichait l'ancien mdp alors que le vrai est le nouveau. **Fix** : ne préserver `clear` que si le hash distant est identique au hash local (`loc.h === _merged[uid].h`). Sinon, abandonner le clear local (obsolète). (2) **P0 mineur `_dcPending` stuck après exception import** : en cas d'erreur pendant import + rafale SSE concurrente, `_dcPending` pouvait rester à `true` → dc() ne se rejouait plus. **Fix** : reset `_dcPending=false` dans le catch de `doImport` + `setTimeout(dc, 0)` de sécurité. (3) **P2 timeout explicite 5s sur `ipwho.is`** via `AbortController` : sans timeout, fetch pouvait bloquer ~90s avant fallback `ipify`, échouant la condition `Date.now()-last.ts<60000`. Fallback aussi timeouté à 5s. |
| **v9.65** | **Cadre légal monégasque + améliorations IA**. (1) Constantes `LOI_1103` (23 articles en 5 sections : monopole SBM, agrément personnel, interdictions, sanctions pénales, contrôle), `OS_8929` (21 jeux autorisés dont Roulette Monte-Carlo 39 cases), `AM_88_384` (5 chapitres, 21 amendements : mises, jetons, cartes neuves, commissaire, redistribution slots 85%, recettes). Helper `searchLoi(query)`. (2) vConvention nouvel onglet "⚖️ Loi" avec 3 sous-onglets : Loi 1.103 (articles par section), OS 8.929 (grille 21 jeux autorisés), AM 88-384 (chapitres + articles + liste amendements). (3) 2 outils IA supplémentaires : `search_legislation` + `get_authorized_games`. Total outils IA : 30 (25 lecture + 5 admin). (4) Prompts IA enrichis : 16 suggestions contextuelles couvrant planning, stats, jeux, lieux, convention, loi, repos, solde CP. (5) buildIASystemPrompt enrichi : bloc cadre légal (Loi 1.103 résumé + OS 8.929 nb jeux + AM 88-384 points clés). |
| **v9.64** | **JEUX_SBM (règles Formation SBM 2016) + vConvention Jeux enrichi + 2 outils IA supplémentaires**. (1) Constante `JEUX_SBM` : 6 jeux détaillés (BJ, RA, Craps, PB, THU, TCP) avec sections pédagogiques complètes, tableaux de tirage (Punto Banco), rapports de paiement (Roulette), paiements Trips/Blind (THU), paiements Ante Bonus/Pair Plus (TCP), lexique poker, procédures croupier TCP (7 sections : tables avec/sans sabot, déroulement main, change, floorman, fausse donne, ordre paiements), hippodrome + annonces (Roulette). Source : 6 documents Formation Jeux SBM mars-nov 2016. Helper `searchJeuxSBM(query)`. (2) vConvention onglet Jeux enrichi : toggle "⚡ Express" (JEUX résumé) / "🎓 Formation SBM" (JEUX_SBM détaillé). Si un jeu a des données SBM, les 2 onglets sont affichés. Le mode SBM montre les sections, rapports, tableaux, lexique, procédures avec mise en forme riche. (3) 2 nouveaux outils IA : `get_game_rules_sbm` (règles complètes Formation SBM d'un jeu) + `search_game_rules` (recherche par mot-clé dans les règles SBM). Total outils IA : 28 (23 lecture + 5 admin). |
| **v9.63** | **Tool use IA custom — 26 outils (21 lecture + 5 admin)**. L'IA Claude API intégrée peut interroger et agir sur TOUTE l'app en temps réel. Constante `IA_TOOLS[]` (26 outils avec JSON schema). Fonction `_iaExecuteTool(name, input)` exécute localement. `callClaudeIA._doFetch` enrichi : route les tool_use custom vers l'exécuteur local + Anthropic web_search. Prompt système enrichi bloc "TES OUTILS" listant les 26 capacités + règles (toujours utiliser, jamais deviner). **Outils lecture (21)** : get_planning, search_employees, get_employee_info, get_stats, get_today_status, find_game_rooms, search_convention, get_team_roster, get_conflicts, get_upload_requests, get_who_is_free, get_leave_balance, get_all_teams, get_audit_log, get_online_users, get_monthly_summary, get_game_rules, get_salary_grid, get_absences, get_rotation_rules, get_documents. **Outils admin (5, guard AID)** : set_planning_code, set_employee_team, admin_set_employee_field, admin_validate_upload, admin_reject_upload. Chaque tool call loggé via `_audit("ia_tool", ...)`. Sécurité : non-admin reçoivent "⛔ INTERDIT" si tentative de modification. |
| **v9.62** | **Grosse version multi-axe : IA enrichie + sécurité + galerie salons + upload modéré + plans architecturaux + iOS fix + bulles chat**. (1) **Bulles chat quasi-transparentes** : fond rgba .09-.22 au lieu de .85-.96 + backdrop-filter blur 18px + text-shadow puissant (0 1px 2px rgba(0,0,0,.9)) + font-weight 600-800 + couleurs nom distinctives (moi doré #ffdc40 / autre vert #a0e080 / DM violet #d0a0ff). Photo de fond visible sans sacrifier la lisibilité. Boutons action chat toujours visibles (pas hover-only, fond rgba(0,0,0,.28) + border), date separator amélioré. (2) **Fix iOS auto-zoom** : `@media (pointer:coarse)` force font-size 16px sur tous les inputs des devices tactiles. Chat-bar .inp et chatSearchIn passés à 16px. Résout le zoom auto iOS Safari. (3) **CSP élargie** : img-src autorise `asset.montecarlosbm.com`, `commons.wikimedia.org`, `upload.wikimedia.org` pour la galerie photos. (4) **Upload modéré complet** : employés soumettent via `employeeProposeUpload()` dans vPartage → demande status "pending" → alerte admin (notif navigateur + toast + badge compteur vAdmin) → admin valide `adminValidateUpload()` (crée doc réel) ou rejette `adminRejectUpload()` (avec raison). Toggle global `setUploadEnabled()` on/off. Store `A.uploadRequests` + `A.uploadEnabled` sync Firebase (FB_FIX). Vue `vUploadRequests()` (compteurs + cards pending/archive + boutons). Route `uploadreq`. Audit complet (upload_request/approved/rejected/toggle). (5) **PLANS_CMC + PLANS_CDP** : constantes architecturales complètes. CMC : 9 salles (Renaissance, Atrium, Europe, Amériques, Blanche, Médecin, Touzet, Super Privés) + 6 annexes + historique + architectes + dress codes. CDP : 4 zones (principale, Electronic Gaming, 2 terrasses) + chiffres (640 machines, 18 postes roulette élec, jackpot 1M€) + niveaux bâtiment. Helper `findSallesPourJeu(jid, etab)`. (6) **buildIASystemPrompt enrichi** : injection PLANS complets (CMC + CDP + différences clés) + règles upload modéré + règles modification (admin-only) + alerte pending uploads. `max_tokens` 1024→4096. (7) **vConvention nouvel onglet "🏛️ Lieux"** : 3 sous-onglets CMC (9 salles extensibles), CDP (4 zones + chiffres + niveaux), Comparer (tableau side-by-side). (8) **Galerie 75 photos salons** : constante `SALON_PHOTOS` (75 photos SBM officiel + Wikimedia CC + archives Rijksmuseum/LOC), helpers URL `_wcUrl/_sbmUrl/_sbm2Url/_sbm3Url/_sbm4Url`, vue `vGalerie()` (grille responsive 240px, filtres étab/source/salon, recherche texte, lazy loading, lightbox clavier ← → Esc, compteur, fallback erreur). Route `galerie`. Raccourci accueil. (9) **15 tests unitaires ajoutés** : isUploadEnabled, countPendingUploads, findSallesPourJeu, SALON_PHOTOS catalogue, PLANS_CMC/CDP, _safeUrl, isDocShared, empLabel senior, esc null-safe. |
| v9.67 | **Version majeure — 35+ fonctionnalités ajoutées en une session**. CORRECTIONS : fix email OTP (toast honnête + config EmailJS), fix REVOLLON (CP j16-26 + AF j27-30), fix IA (type:custom supprimé + calcStats scope + 6 nouveaux outils = 36 total), fix clignotement (anti-boucle dc + _fbInitialLoad), fix transparence (48 backgrounds opaques → semi-transparents + fix flash), fix iOS zoom (double protection), fix import (matching renforcé + validation post-import 5 règles). SÉCURITÉ : TTL heartbeat 2min, fbApplyData validation type, delEmp nettoyage complet, rate-limiting mots de passe, indicateur force mdp inscription. FONCTIONNALITÉS : splash screen, Firebase différé, auto-save fiches (onblur 9 champs), admin édite tous champs, CODE_HOURS complet, calendrier enrichi (couleurs + horaires clairs), solde CP (barre progression accueil + planning), notifications précises (jour + ancien→nouveau), dashboard RH (absentéisme + présence + CP/équipe + courbe 12 mois SVG), TTS lecture vocale, micro dictée (STT), compte visiteur U007, compteur connexions/vues, likes documents, mode offline (SW stale-while-revalidate), thèmes visuels (Casino/Clair/Nuit), export PDF planning, modèles planning (templates), multi-langues FR/EN/IT, recherche universelle (topbar), swipe mois (geste tactile), animations micro-interactions, admin réorganisé (7 catégories), PWA installable (manifest.json + icône), demandes employés (congé/échange/modification), mode présentation (plein écran), donut SVG stats, favoris collègues, cartes intelligentes accueil, timer shift live, raccourcis clavier, IA locale enrichie (jeux/convention/congés/rotation/lieux), ressources intégrées vDocs, script anti-régression (.github/scripts/verify-build.sh). AUDIT : 23/23 PASS. |



---

# Historique versions récentes (déplacé de CLAUDE.md 2026-07-10)

## Historique versions récentes

> 📖 **Historique complet** dans `CHANGELOG.md` à la racine du dépôt (v8.83 → v9.67 archivé).

| Version | Changements |
|---------|-------------|
| **v9.854 / page CMCteams light v1.25 / kdmc-router** | **🔔 Chat « Joindre Kevin » — push app FERMÉE + accusé de lecture (Kevin 2026-07-03 « Go tout et va encore plus loin »)**. (a) **Push même app fermée** : la page POST `/__notify-kevin` (routeur kdmc, même origine) → `handleNotifyKevin` relaie vers le worker de push existant (`apex-push-worker /send-all`, **jeton gardé SERVEUR**, jamais exposé) → Kevin reçoit une vraie notif push même CMCteams fermé (via ses abonnements kdmc/Apex). Anti-spam : throttle KV 12 s ; corps générique tronqué ; garde `ROUTES[host]` (403 hors kd-mc.com) ; fail-open total. `notifyPush` généralisé (opts tag/url). (b) **Accusé de lecture** : Kevin ouvre l'inbox → `vKevinInbox` écrit `cmc_dep_read[dkey]=ts` (FB_FIX, seulement si la map change) → l'employé voit **« ✓ Lu »** sur ses messages (page lit `cmc_dep_read` dans `_depPullReplies`). Mesuré (Playwright) : 0 erreur JS, envoi OK, « ✓ Lu » rendu ; `node --check` worker OK. Tests kevin-inbox/gate/live 12+10+12 OK, `test:ci` EXIT 0. Déploiement routeur via `deploy-kdmc-router.yml`. |
| **v9.853 / page CMCteams light v1.24** | **📷 Chat « Joindre Kevin » — photo + notifs + contraste (Kevin 2026-07-03 « Go tout et va plus loin » + capture « corrige la couleur, on voit rien »)**. (a) **Photo** : bouton 📎 → downscale canvas (max 1200px, JPEG ≤360 Ko) → stockée dans un node DÉDIÉ `cmc_dep_img/<id>` (chargée à la demande côté app via `cmcViewDepImg`), JAMAIS dans les tableaux inbox (qui restent légers) ; la bulle porte `hasImg`+`imgId`, bouton « 📷 Voir la photo » dans `vKevinInbox`. (b) **Notif Kevin** : nouveau message employé → `_checkKevinInbox` (hook `fbApplyData` sur `cmc_kevin_inbox`) → `sendNotif` (PWA/onglet) ou toast si visible, anti-doublon par ts. (c) **Notif employé** : réponse de Kevin → `Notification` navigateur (opt-in au 1er envoi). (d) **Contraste corrigé** : bulles en texte clair sur fond sombre (envoyé `#ffe9a8` sur `#3a2e08`, horodatage `#e8cf86`) — l'ancien gris-sur-or était illisible. Mesuré (Playwright) : 0 erreur JS, photo → PUT `cmc_dep_img` (hors inbox), `<img>` rendu, couleurs lisibles. `test:ci` EXIT 0. |
| **v9.852 / page CMCteams light v1.23** | **💬 CHAT « Joindre Kevin » sur la page CMCteams light, une fois connecté (Kevin 2026-07-03 « donne accès au chat apex une fois connecté, pour me joindre si besoin. Va plus loin »)**. L'employé CONNECTÉ (gate franchi, sinon bouton caché) a un **bouton flottant 💬 → panneau de conversation**. Envoi via `window._depSendKevin` (dans l'IIFE Firebase de la page, réutilise l'auth anon) : le message arrive (a) dans l'**inbox admin CMCteams** (`cmc_kevin_inbox`, vue `vKevinInbox`) ET (b) dans le **pont Apex** (`ax_cmc_kevin_inbox`, que le chat Apex de Kevin lit) — GET frais → unshift → PUT (anti-clobber #65/#109) + filet append-only `cmc_dep_msgs/<id>` (jamais clobbé). **Va plus loin = 2 VOIES** : Kevin répond depuis l'app (bouton **↩️ Répondre** dans `vKevinInbox` sur les messages page, `cmcReplyToDep`/`cmcReplyDepModal`) → `cmc_dep_reply[<dkey>]` (FB_FIX) → l'employé voit la réponse **dans le même panneau** (page lit `cmc_dep_reply`, SSE live, toast). `dkey` = clé stable `NOM_PRÉNOM` (identique des 2 côtés, même dérivation que `cmc_dep_access`). Nodes DÉDIÉS, ne touchent JAMAIS `cmc_ov`/le planning. Fail-open, mobile (safe-area, Entrée=envoi). **Mesuré (Playwright, Firebase mocké)** : 0 erreur JS, bouton caché avant gate / visible après, envoi → PUT `cmc_kevin_inbox`+`ax_cmc_kevin_inbox`+`cmc_dep_msgs`, bulle rendue. Tests kevin-inbox 12/0, departs-gate 10/0, departs-live 12/0, `test:ci` EXIT 0. |
| **v9.851 / page CMCteams light v1.22** | **🔢 NUMÉROS DE DÉPART — rotation qui se réinitialisait tous les 4 jours (Kevin 2026-07-03 capture CMC Éq.5 « vérifie l'algo des départs, il y a des erreurs »)**. `calcDepPos` (app) ET `compute` (page) faisaient `rot=(wi%4)+Math.floor(wi/4)` → la rotation REMETTAIT à zéro tous les 4 jours travaillés → 2 jours à qq jours d'écart (même effectif) affichaient les **MÊMES numéros** (mesuré sur la capture : **jeu 6 = dim 9** pour les 7 chefs) et chaque chef restait bloqué sur 3-4 places au lieu de faire le tour des 7. **FIX** : `rot=wi` (+off côté page) = **+1 par jour travaillé** → jour 1 travaillé = la séquence dans l'ordre des chefs, chaque jour suivant décale de 1, tour complet en `pc` jours, **aucune répétition dans un cycle**. **Sûreté (lesson #112/#116)** : reproduit à l'identique le cas que Kevin avait validé dans l'historique (FAUTRIER = Départ 4 le 25 : les 2 formules coïncident là car pc=4 y annule l'écart `3·floor(wi/4)`), donc ce fix ne re-casse PAS ce repère. **Preuve MESURÉE** : reproduction locale de la capture d'août (CMC Éq.5, 7 chefs) = 100 % identique à ce que voit Kevin ; après fix, jeu 6 ≠ dim 9, jours 1-6 inchangés, jours 7+ tournent proprement. Test `test:departs-algo` étendu : référence page alignée sur `rot=wi` + **verrou anti-répétition** (2 jours à roster complet dont `wi` diffèrent de `k` avec `0<k<pc` DOIVENT différer — exactement ce que l'ancienne formule violait). `test:departs-sync` (app==page) OK, `test:ci` EXIT 0. |
| **v9.850** | **🎴 Chef baccara → planning CHEFS (plus dans les cartes) (Kevin 2026-07-02 « EHRET G, BATTAGLIA D sont chef baccara → devraient être dans les plannings chefs, pas en équipe carte »)**. EHRET G / BATTAGLIA D ont `emp.family` DEF_EMP = **baccara** (Punto Banco, pas de section propre dans les PDF). La garde anti-phantom `emp.family==="baccara"→"cmc"` (lesson #104, dans `familyForMonth` ET `_cmcDeriveFamily`) les envoyait en **cartes** dès que `familyHistory[key]` était absent (repli, après le wipe one-shot). Or ce sont des **CHEFS** (codes journaliers majoritairement suffixe `c`), section « Chefs black Jack » du PDF. FIX : nouveau `_cmcBaccaraFallback(emp,key)` — un DEF_EMP baccara dont les codes du mois sont **majoritairement chef (`c`)** → **bj** (planning chefs) ; un croupier baccara (codes sans `c`) → **cmc** (cartes). Le suffixe `c` = RÔLE (lesson #107), utilisé ICI uniquement pour trancher entre les 2 replis d'un baccara, jamais comme famille directe. Mesuré (import juillet réel, familyHistory forcé absent) : EHRET G/BATTAGLIA D → **bj**, croupier baccara → cmc, chef de roulette (emp.family roulettes) inchangé. Test `test:baccara-chef` (7/0) câblé `test:ci`. Seed + boards régénérés. kevin-truth 10/0, team-rule 16/0, vplan 9/0, departs-sync OK. `test:ci` EXIT 0. |
| **v9.849** | **🌱 École roulette Europe INTÉGRÉE DIRECTEMENT dans l'app (seed) + la page (boards) — Juillet ET Août (Kevin 2026-07-02 « Import toi directement dans l'app et la page » + « Fais pareil pour août »)**. Kevin ne veut pas ré-importer à la main. Le SEED partagé (`tools/shared/planning-seed.js`, chargé par l'app pour les mois sans données live, lesson v9.824) porte désormais un tableau `ecole[]` = les employés carte KE reclassés roulettes ; l'app pose le **marqueur STICKY** `emp.ecoleRoulette[key]` à l'application du seed → ils RESTENT roulettes après le wipe familyHistory one-shot + la détection différée (sinon re-teamés en cartes). Régénéré depuis les VRAIS PDF (même pipeline que v9.848, école baked-in) : **Juillet 32 école→roulettes**, **Août 33 école→roulettes**. Boards page (`boards-gen.js`) déjà régénérés v9.848 (école dans familyHistory). Nouveau test `test:seed-ecole` (12/0 : seed porte les KE + sticky survit au wipe, Juillet+Août, contrôles NON-KE restent cartes) câblé `test:ci`. Livrable : sur un device sans données live (install/nouvel appareil) ET la page Départs partagée, les cartes fond bleu apparaissent en roulettes SANS ré-import. `test:ci` EXIT 0. |
| **v9.848** | **🎡 École roulette Europe (fond bleu = compétence KE) → roulettes, DÉTECTION FIABLE + STICKY (Kevin 2026-07-01 captures IMG_2957/2958 « pareil pour les autres avec le fond bleu »)**. Le v9.846 reclassait les cartes « fond bleu clair » vers roulettes via un tag PIXEL `{{ECOLE}}` qui ne se produisait jamais (couleur cyan trop pâle, `b≈g`) → **1/28 seulement**. VÉRITÉ mesurée sur le PDF réel : le fond bleu clair est posé EXACTEMENT sur les employés carte dont le code-poste contient **`KE`** (`.BRTCP+KE` / `.BRTCPKE` = compétence roulette Europe), écrit noir sur blanc dans le texte du PDF (reproduction fidèle, 0 pixel-tuning). FIX détection : `_cmcApplyEcoleToRoulettes` détecte l'école via le **code-poste `KE`** dans la source (le tag pixel reste accepté en secondaire) ; garde `.BRTCPK` (K SANS E) → PAS de faux positif ; famille cmc-only + anti-homonyme #38. FIX persistance : le reclassement était **écrasé** par le wipe familyHistory une-fois `cmc_v805_famreset` (deferred) → rest-pattern les re-teamait en cartes (mesuré : 27/28 revenaient cmc, seul MIANI 16-31 survivait par timing). Rendu STICKY via marqueur `emp.ecoleRoulette[key]` respecté par (a) le wipe une-fois, (b) `_cmcDeriveFamily` (autorité finale, prime même sur la section). **Mesuré import juillet-2026-v2 réel, session fraîche (wipe actif) : 28/28 école → roulettes, 7/7 contrôles NON-KE restent cmc.** Tests `test:ecole` (9/0, dont KE / K-sans-E / garde famille / homonyme / pixel secondaire), `test:team-rule` 16/0, `test:departs-sync` (couverture 291/280, BJ+miroirs alignés) — boards régénérés (juillet 44 équipes). Méthode : lessons #67/#83/#115 (signal texte fiable > pixel ; prouver sur données réelles ; sticky vs re-dérivation). `test:ci` EXIT 0. |
| **v9.847** | **🎯 Dispatch équipe/miroir par les 3 premiers codes travail (fin des équipes surpeuplées) (Kevin 2026-07-01 « Vérifie l'équipe 5 par exemple, bcp trop de personnes. Je pense équipe et miroir regroupés. Faire attention aux horaires du 1er et du 3ème jour pour dispatch »)**. La signature d'équipe de `_cmcDetectTeamsByRestPattern` était `famille|repos|SEUL 1er code travail` → quand une équipe et son MIROIR partagent les mêmes repos ET le même 1er code, elles **fusionnaient** en une équipe surpeuplée (mesuré sur juillet réel : **cartes équipe 5 = 21 personnes**). Les employés réels divergent dès les **jours 1-3** (ex cartes : jour 2 `16/22` vs `16/3`, jour 3 identique). FIX : signature = les **3 premiers codes de TRAVAIL** (base sans suffixe `c/'/"/*`, en sautant repos/absences). **Raffinement MONOTONE** (le 1er code reste le 1er élément) : ne fusionne JAMAIS ce que l'ancien séparait, ne fait que **séparer** les équipes+miroirs mal fusionnées ; membres d'une même équipe (chef suffixe strippé, CP-partiel absences sautées) → même séquence → regroupés. **Mesuré sur juillet réel** : cartes équipe 5 **21→10**, **0 fragment** (aucun singleton créé), **+10 miroirs** détectés (26→36), couverture identique (292), crosscheck BJ+miroirs aligné. Test `test:team-rule` (16/0, +4 cas dispatch : équipe X et miroir X' même 1er code mais jour 3 différent → SÉPARÉS + lien miroir). Boards page régénérés (44 équipes juillet). **Méthode** : OLD vs NEW mesurés (lesson #83), N=3 choisi (SEQ3=SEQ4=SEQ5 convergent). `test:ci` EXIT 0. **Bonus page CMCteams light v1.21** : la régénération des boards (nouveaux miroirs) a révélé un bug — `applyBoards` supprimait les BOARDS committés d'un mois live mais gardait ses MIROIRS committés → un miroir stale (`2026-07-1↔2026-07-16`) survivait et son reverse écrasait le miroir live → FIX : retirer aussi les miroirs committés d'un mois live (live seul fait autorité, lesson #117). `test:departs-live` 12/0. |
| **page CMCteams light v1.20** | **🔐 Gate 1ère connexion (CGU + fiche) sur la page partagée (Kevin 2026-07-01 « J'ai partagé la PWA CMCteams light. Les personnes se connectent sans rien valider. Rien n'est demandé. Cgu, fiche, rien »)**. La page (lien partagé) laissait entrer sans RIEN demander. Ajout d'un **overlay bloquant à la 1ère ouverture** : **prénom + nom** (règle LOGIN TOUJOURS PRÉNOM + NOM, ≥2 lettres chacun) + **case CGU** (« outil interne SBM, nom/prénom pour ouvrir ton équipe et savoir qui consulte, aucune donnée partagée à l'extérieur ») → bouton « Accéder aux départs ». Refus tant que prénom/nom manquants OU CGU non cochée. À la validation : identité+CGU stockées (`cmc_dep_identity`), **fiche loggée Firebase** (`cmc_dep_access/<clé>` = nom/prénom/appareil/heure → l'admin voit QUI consulte, best-effort, ne touche jamais `cmc_ov`), ME calé sur « NOM I » pour ouvrir son équipe. **Reconnu auto ensuite** (plus jamais redemandé sur l'appareil). **Fail-open** : toute erreur → overlay caché (jamais de lockout, règle #55/#81). `?me=` pré-remplit le nom (CGU reste requise). Test `test:departs-gate` (10/0 : overlay affiché, refus vide/sans-CGU, validation → accès+stockage+log, retour reconnu auto) câblé `test:ci`. Page bump v1.20 (badge + version.txt → MAJ auto force). |
| **v9.846** | **🎡 Employés carte « fond bleu clair » = école roulette Europe → équipes roulettes (Kevin 2026-07-01 « dans les employés carte, tous ceux avec le fond bleu clair doivent être intégrés dans les équipes roulettes par rapport au repos… Ce sont des employés qui sortent de l'école roulette Europe et s'intègrent dans les équipes. Les enlever des équipes cartes »)**. Les diplômés de l'école roulette Europe apparaissent dans la section CARTES du PDF avec un **fond de nom bleu CLAIR** (distinct du bleu MOYEN {{FF}} faisant-fonction et des codes bleus 20/5/DELTTRA). (1) **Détection raster** : chaque item PDF porte déjà `i.bg` ; un fond **bleu clair brillant** (`b≥r+15 && b≥g+8 && b>190 && r>130`) sur un texte type **NOM** (`^[A-Z][A-Z\s.\-]{2,}$`, pas un code horaire) → tag `{{ECOLE}}`. (2) **Reclassement** `_cmcApplyEcoleToRoulettes(key,txt)` (AVANT `_postValidateImport`) : pour chaque nom `{{ECOLE}}` qui **correspond à un VRAI employé** (surname + initiale anti-homonyme #38) **actuellement en famille cmc/cartes**, passe `familyHistory[key]="roulettes"` + **efface `teamHistory[key]`** → la détection par repos (`_cmcDetectTeamsByRestPattern`) les place dans l'équipe roulettes dont les repos correspondent, et ils quittent les équipes cartes. **SÉCURITÉ (reproduction fidèle)** : n'agit QUE sur un tag matchant un employé réel + en cmc → un code bleu (DELTTRA) ou faux positif = ignoré (aucun employé). `{{ECOLE}}` strippé par `cleanText` avant parsing (comme {{FF}}/{{CONV}}, pas de fuite lesson #115). Banner récap + audit `ecole_roulette_reassign` (noms déplacés). Test `test:ecole` (8/0 : cmc→roulettes, team effacée, non-marqué intact, roulettes intouché, homonyme rejeté, DELTTRA ignoré) câblé `test:ci`. Fixtures actuelles sans nom bleu → 0 déplacement → 0 régression (fidélité 0 écart, everyone-has-planning OK, vplan 9/0, team-rule 12/0). |
| **v9.845 / page CMCteams light v1.19** | **🎯 MÊMES DÉPARTS POUR TOUS + rotation synchronisée + MAJ auto force sur la page (Kevin 2026-07-01 « Pourquoi les autres non pas les mêmes départ sur leurs app… Ils n'ont pas les mêmes sur leurs CMCteams que sur le miens. Dois se mettre à jour partout si je modifie » + « Vérifie aussi la Maj auto force partout » + « iOS et Android toujours »)**. Deux causes, deux surfaces. (1) **App (index.html)** : `calcDepPos` ET le rendu `vDeparts` INJECTAIENT le spectateur (`A.user`) dans le roster de rotation → la base d'index + les présents variaient SELON QUI REGARDE → chaque employé calculait des numéros DIFFÉRENTS. Or l'ordre de départ est un fait OPÉRATIONNEL PARTAGÉ. Injection supprimée des 2 endroits → roster 100% déterministe = CHEFS_T[tid] (sync `cmc_chefs_t` FB_FIX) + CI (`cmc_ci_*` FB_PRE). De plus `vDeparts` utilisait encore l'ANCIEN algo inline (refEmpD+taille fixe = 34/38 équipes divergentes, lesson #116) → remplacé par un appel à `calcDepPos` (source UNIQUE, algo page validé) ; `_wiMap` d'ombrage passé en UNION des jours travaillés. Prouvé (Playwright, vrai import Août) : **0 écart** entre admin/employé/personne sur 4666 cellules, app==page inchangé, grille rendue identique. (2) **Page CMCteams light (tools/departs)** : l'ancrage (`off`), la base de départ (`ci`) et les marques NR/5e (`nr`/`cq`) étaient des préférences LOCALES (`localStorage[B.key]`, jamais synchronisées) → l'admin cale une rotation, personne d'autre ne la voit. Node Firebase dédié **`cmc_dep_cfg/<board>`** = `{off,ci,nr,cq,ts}` : `save()` PUSH (debounce 500ms), `_fbPullCfg` + SSE `cmc_dep_cfg.json` + focus/visibility APPLIQUENT sur tous les appareils (ne touche JAMAIS `cmc_ov`). (3) **MAJ auto force** ajoutée à la page (elle n'avait qu'un bouton manuel) : sonde `version.txt?_v=` (cache:reload) au boot + 60s + focus/visibility → si version distante ≠ APP_VER : unregister SW + clear caches + `location.replace(?_force_upd_)` (pattern cross-platform iOS+Android, pas dépendant du SW updatefound iOS). Badge = APP_VER (lesson #103). Tests : `test:departs-algo` (10/0, +déterminisme cross-spectateur +rendu), `test:departs-cfg` (7/0, PUSH+PULL rotation partagée), `test:departs-live` (12/0), `test:departs-sync` (292/292 après régénération boards). `test:ci` EXIT 0. |
| **v9.841** | **📇 Fiche perso COMPLÈTE par employé (Kevin 2026-07-01 « l'historique remonte dans chaque fiche perso avec toutes les infos possibles » + « Oubli pas la map, position live, historique, travail, question, effectué demandé »)**. Nouvelle vue admin `vFichePerso(uid)` (route `fiche`, guard admin) qui agrège TOUT pour une personne : (1) **Identité** (matricule, email, poste, naissance, équipe du mois, USBM depuis `A.reg`) ; (2) **Travail ce mois** (compteurs travaillés/repos/absences depuis `A.overrides` + code du jour + **n° de départ** via `calcDepPos`) ; (3) **Position & présence** (🟢 en ligne via `getOnlineUsers` + dernière position `cmc_positions` + lien Google Maps + bouton **map des tables** `pitmap`) ; (4) **Historique connexions** (15 dernières de `cmc_userlog` : type, appareil, OS, ville/pays, 📍 GPS) ; (5) **Demandes** — effectué/demandé (`A.exchanges` + `_empRequests`) ; (6) **Questions/messages** envoyés à Kevin (`cmc_kevin_inbox` filtrés). Ouverture 1-clic : nom + bouton « 📇 Voir la fiche complète » dans la boîte `vKevinInbox`, ET bouton « 📇 Fiche complète » dans chaque carte employé de `vEmps`. Doublon `_cmcRelTime` supprimé (leçon #108). Test `test:fiche` (19/0 : 6 sections rendues, guard admin, route vMain, uid introuvable propre, pas de doublon) câblé `test:ci`. EXIT 0. |
| **v9.840** | **💬 « Écrire à Kevin » — message direct employé → routé vers Apex (Kevin 2026-07-01 « Intègre dans la page un chat, message direct à moi par les personnes si problème ou question. Passe par mon chat apex »)**. Nouveau `cmcSendToKevin(text)` : écrit le message dans la boîte admin CMCteams (`cmc_kevin_inbox`, vue `vKevinInbox` avec badge « N nouveaux » sur l'accueil admin) ET dans le chemin PARTAGÉ cross-app `ax_cmc_kevin_inbox` (les 2 dans `FB_FIX` = sync) → **bridge Apex** (le chat Apex lit ce chemin). Bouton **« 💬 Écrire à Kevin (problème/question) »** sur l'accueil de CHAQUE employé → modale textarea (pas de `prompt()` natif, clavier iPhone). Test `test:kevin-inbox` (12/0 : 2 boîtes écrites, FB_FIX, statut, tag app, vue admin, refus non-admin, modale) câblé `test:ci`. |
| **v9.839** | **🔓 CGU/autorisations tout-en-1-clic (Kevin 2026-07-01 « Les cgu accepté pour tout d'un clic ? Loca, caméra, micro etc »)**. Avant : consentement feature par feature. `cmcRequestAllPermissions()` : un seul clic pose le CGU des 5 features ET déclenche les demandes natives iOS à la suite (notifications → géoloc → caméra+micro ; iPhone accorde/refuse chacune, le refus d'une n'empêche pas les autres). Bouton « 🔓 Tout autoriser en 1 clic » dans Vie privée & RGPD + récap toast. Testé Playwright (5 flags posés, notifs+géoloc accordées). `test:ci` EXIT 0. |
| **v9.838** | **🔐 Face ID « reconnu auto » — plus besoin de retaper U11804 (Kevin 2026-07-01 « il n'y a pas de FaceID dans ma pwa CMCteams, je dois mettre mon U11804 tjs »)**. Le boot effaçait `cmc_uid` après le TTL 8h → Kevin (qui ouvre l'app ~1×/jour) retombait TOUJOURS sur le formulaire nom/prénom/matricule. FIX (règle RECONNAISSANCE AUTO + lesson #81/#827) : (1) `cmc_last_uid` PERSISTANT (jamais effacé par le TTL, dans `FB_LOCAL` = jamais sync Firebase, anti-impersonation lesson #40) posé à chaque login ; (2) au boot, si session expirée mais `cmc_last_uid` a **Face ID enrôlé** sur cet appareil → `vLogin()` **saute directement à l'écran Face ID** (`loginStep=1`, `loginUid` pré-rempli) → Kevin ouvre l'app → Face ID d'un regard, **0 saisie** ; repli mot de passe + « ← Retour » conservés (**jamais de lockout**, fail-open lesson #55/#81) ; (3) l'offre d'enrôlement `_cmcOfferFaceIdEnroll` ajoutée au chemin admin direct (16338) qui la sautait. **1ère connexion = code (enrôle Face ID), ensuite Face ID auto.** Test `test:faceid` étendu 10→17 (saut, pré-remplissage, repli, fail-open non-supporté/non-enrôlé, `cmc_last_uid` FB_LOCAL). `test:ci` EXIT 0. |
| **v9.837** | **🎨 DELTTRA/PRT/etc reproduits à l'identique partout + ajoutés aux légendes (Kevin 2026-07-01 « reproduit à l'identique partout DELTTRA, PRT, etc » + « Tu as tout les documents… Ajoute aux légendes »)**. (1) **Couleurs** : `cmcCodeStatusBg` (app) + `codeStyle` (page Départs) reproduisent enfin la couleur PDF des codes spéciaux au lieu de les washer en blanc — DELTTRA → bleu clair (cellule bleue du PDF v9.829), DELPT → beige (déplacement), MT/AT/SS/CL/ABS/ABI/CSS/EDC → gris. **Bug EDC corrigé** : le strip `[C'":*]$` effaçait le `C` final (« EDC »→« ED »→blanc) → traité sur le code brut. (2) **Légendes** : 15 codes SBM (`BULLETIN_CODES`, Note DAJS 6/1/1993) présents dans les PDF mais absents du dict `CODES` (donc ni reconnus ni dans la légende) ajoutés avec le **libellé officiel** (RTP/RTR/DEP/AT/MT/SS/CSS/CL/ABS/ABI/EDC/CFL/CRH/CDH) ; la pastille de légende prend désormais la couleur RÉELLE de la cellule (`cmcCodeStatusBg`) → **légende À L'IDENTIQUE des cellules** ; + repère Convention (fond rouge) / horaire modifié. Codes reconnus = pas de décalage de ligne (lesson #69). Test `test:code-legends` (86/0 : couleur app≡page par famille + présence CODES + présence légende) câblé `test:ci`. Page v1.14.1. `test:ci` EXIT 0 (cross-check couverture/horaires/BJ inchangés → 0 régression). |
| **v9.836** | **⚡ Fin des blocages + Baccara→CMC + couleurs Départs + boards régénérés (Kevin 2026-07-01 « l'appli est très lente et bloque partout, pas que démarrage/import » + « il y a encore la famille Baccarat » + IMG_2948 « les départs doivent respecter les mêmes couleurs »)**. 5 correctifs mesurés. (1) **PERF — détection d'équipes différée hors du rendu** : `dc()` recalculait les équipes (~79 ms serveur ≈ 300 ms iPhone) DANS le chemin de rendu synchrone à chaque invalidation de cache (navigation, SSE) → gel de l'écran « partout ». Déplacée dans un `setTimeout(0)` (flag `_cmcDetectScheduled`) : `dc()` rend TOUT DE SUITE puis la détection tourne au tick suivant et déclenche UN SEUL re-render. **Mesuré : dc() cache-hit 89 ms→0.1 ms.** La chaîne différée reproduit la chaîne COMPLÈTE de `_postValidateImport` (`…RestPattern` + `_cmcFallbackUnassignedTeams` + `_cmcApplyMaladieLongueDuree`) — sinon le wipe effaçait les équipes d'ABSENCE (GARRO→congés, MIRANDA→maladie) posées à l'import (régression rattrapée par `test:vplan`). (2) **Baccara → CMC/cartes** : vPlan ne rend plus de section fantôme « Bac. » — famille du mois = `familyForMonth` (section du PDF = autorité, lesson #67/#107) + garde anti-phantom (DEF_EMP « baccara » Punto Banco → cmc, v9.802) ; `FAMS_ORDER` sans baccara, helper `_planFamOf`, `_empFamLabel` normalisé, boutons Tout ouvrir/fermer. **Vérifié import réel : 0 section Baccarat, 13 équipes CMC (c3–c16) rendues** = les « équipes employés carte manquantes » étaient mal étiquetées (Baccarat), pas absentes → PAS touché le seuil sensible `<10 cellules` (lessons #104/#112/#113). (3) **Couleurs Départs = PDF** : `vDeparts` applique `cmcCodeStatusBg` (travail BLANC, RH violet, R lavande, CP rose, CDP orange, AF vert, M rouge clair, RRT jaune) + méta pixel (convention rouge, `fg:red`→écriture rouge sur blanc), à l'identique de vPlan/du PDF ; la pastille de départ colorée reste par-dessus (info de rotation). (4) **Bruit console** : le battement présence SSO `/__sso/whoami` ne fire plus hors `http(s)`+`*.kd-mc.com` → `test:runtime` 100/100 (0 erreur, était rouge sur main). (5) **Boards Départs régénérés** : la détection d'équipes différée capture désormais de façon déterministe les ~68 employés ABSENTS tout le mois (CP/M) → ajoutés à leurs groupes Congés/Maladie (couverture 254→280/291), **0 retiré, 0 équipe de travail perturbée** (diff mesuré). `test:ci` **EXIT 0** (160/160 + vplan 9/9 + conv-colors 5/5 + departs-algo 6/6 + departs-sync couverture/horaires/BJ OK + verify 18/18 + kevin 10/10). |
| **page v1.14.0 (sync app⇄page)** | **🔄 La page Départs et l'app CMCteams s'actualisent AUTOMATIQUEMENT, dans les deux sens, AUSSI entre appareils (Kevin 2026-06-29 « les deux côtés s'actualisent auto si une modif d'un côté ou l'autre », « aussi entre appareils »)**. Avant : la page utilisait un snapshot figé (`boards-gen.js`) que je régénérais à la main → elle ne voyait pas les imports/modifs de l'app. **App→page** : la page lit désormais le planning LIVE de l'app (`cmc_e`/`cmc_ov`/`cmc_ov_meta`/`cmc_team_mirror_*`) en **local** (même origine localStorage, instantané) ET via **Firebase** (cross-device, auth anonyme + SSE sur `cmc_ov`), et reconstruit les boards à l'identique de `_gen-boards.mjs` (`buildBoardsFromState`). Les boards committés restent le **repli hors-ligne** / mois sans données live (la page marche toujours sans réseau). Auto-refresh sur SSE Firebase + `storage` (autre onglet) + `visibilitychange`/`focus`. **Page→app** : une cellule corrigée sur la page (`editCell`) remonte dans `cmc_ov` — la page re-lit le `cmc_ov` COMPLET, modifie la cellule, et **PUT l'objet ENTIER** (le SSE de l'app applique `path="/cmc_ov"` → `fbApplyData`, cross-device). **Anti-clobber (lesson #65/#109)** : on n'écrit QUE si le mois édité existe déjà dans le planning app → JAMAIS de PUT partiel qui écraserait le planning. App **inchangée** (elle synchronise déjà `cmc_ov`/`cmc_e` et applique les changements distants via SSE). CSP page élargie (`firebasedatabase.app` + `identitytoolkit.googleapis.com`). Nouveau test `test:departs-live` (12/12 : app→page cross-device avec Firebase mocké + page→app whole-object + anti-clobber) câblé dans `test:ci`. |
| **v9.834** | **🎯 NUMÉROS DE DÉPART — l'app alignée sur l'algo VALIDÉ de la page (Kevin 2026-06-29 « vérifie les numéros des départs pour chacun, l'algorithme, les horaires »)**. Audit mesuré (Playwright, vrai import Août) : l'`calcDepPos` de l'app divergeait de la page Départs sur **34/38 équipes** — il était resté sur l'ANCIEN algo (refEmp + taille fixe), alors que la page avait été corrigée par Kevin le 2026-06-22 (« il manque les départs à partir du 16 » + « passer à 4 pour l'algo »). 3 bugs réels mesurés : (a) **AUCUN numéro de départ dès que le 1er chef réf part en CP à la mi-mois** (workDays indexés sur lui seul → wi=-1 pour TOUS — ex équipe 4 : 68/70 cellules de travail SANS numéro à partir du 16) ; (b) **numéros FANTÔMES** > nb de chefs présents (ex LANDAU B j2 = « 4 » avec 2 chefs présents) ; (c) un absent « consommait » un numéro. **FIX** : `calcDepPos` réécrit en réplique EXACTE de `compute()` de la page — (1) workDays = UNION des jours travaillés par tout chef actif, (2) rotation sur les chefs PRÉSENTS le jour J + taille de séquence = nb présents, (3) chaque présent reçoit `SEQd[(rot+rang)%pc]` (rang parmi présents triés par base CI/index), off=0. `DEF_CI={}` côté app = base par index, identique à la page (`ST.ci ?? ai`). **Preuve MESURÉE** : re-probe = **0/38 équipe divergente**, 4646/4646 cellules travaillées avec numéro, 0 null, 0 fantôme, app==page sur CHAQUE cellule. Nouveau test régression `test:departs-algo` (6/6) câblé dans `test:ci` (EXIT 0). Horaires (codes/heures) déjà fidèles (test:fidelity 29/29). |
| **v9.833 / page v1.13.4** | **🎨 Fin du sur-rougissement des codes apostrophe NON-chef (croupiers = BLANCS au PDF) — pixel = seule autorité couleur (lesson #115, Kevin « à l'identique »)**. Le suffixe `'`/`"` n'est PAS un signal de couleur (c'est un marqueur de RÔLE/contrat) : le PDF montre les codes apostrophe des croupiers (ex `22/6'`) en BLANC. Or l'ancienne heuristique `/['"]$/`→rouge survivait dans 6 sites et les colorait à tort en rouge/jaune. **Retiré partout** : `cmcCodeStatusBg` (plus de `/['"]$/`→CONV), `_cmcInferCellMetaFromCodes` (plus de fabrication de méta CONV sur apostrophe), `_cmcEnsureQuoteVariant` + 2 sites parser + entrée codée en dur `"19/4'"` (la variante apostrophe garde le **style du code de base** — texte sombre, fond blanc — au lieu du style Convention rouge/jaune), page `codeStyle`/`cellClass` (plus de suffixe→CONV_COLOR). La **convention (fond rouge)** vient désormais UNIQUEMENT de la méta capturée au PIXEL (`bg:"CONV"`, posée par les parsers via `{{CONV}}`). Compteurs `totConv` laissés sur le suffixe (stat contrat, pas couleur). **Vérif rendu mesurée (Playwright, pas déduite)** : croupier `22/6'`→ **fond `#fbfaf3` blanc + texte sombre** (avant : rouge/jaune) ; **DESARZENS INCHANGÉ** — j5 `19/3c` rouge (méta pixel), j8 `16/3c` texte rouge, j1 `19/4'c` blanc. Boards+seed régénérés (CONV pixel-only : 420 cellules). `test:ci` **EXIT 0** (conv-colors 5/5, cross-check départs OK, seed 8/8, faceid 10/10, fidélité/équipes intacts). ⏭ Reste : couleurs des en-têtes de dates (weekend déjà ombré ; affluence/événement absents des fixtures). |
| **v9.832** | **🎨 Convention/horaire-modifié AUSSI dans « Mon Planning » (vue perso de Kevin) — couleurs pixel à l'identique**. v9.831 colorait `vPlan` + page Départs, mais `vMonPlanning` (la vue PERSO, là où Kevin regarde SON planning) détectait encore la convention par l'ancienne heuristique apostrophe (`_cmcIsConv`) → son `19/3c` (sans apostrophe) n'y était PAS rouge. Nouveau helper `_cmcCellColorClass(eid,d)` lit la META capturée au pixel (`bg:"CONV"`/`fg:"red"`) → vMonPlanning affiche maintenant **fond rouge + badge 🎩 CONV** (jour de R travaillé) et **écriture rouge + badge ✎ modifié** (horaire modifié), comme l'import. Additif (garde l'apostrophe en repli, n'enlève rien). Vérifié visuel (DESARZENS Août : j1/j3 blancs, j5 `19/3c` rouge+CONV, j8 `16/3c` écriture rouge+modifié). App-only (pas de régénération page/seed). `test:ci` EXIT 0. ⏭ Reste (passe suivante) : retirer le sur-rougissement des codes apostrophe NON-chef (croupiers — le PDF les montre blancs) + couleurs des en-têtes de dates (weekend/affluence/événement). |
| **v9.831 / page v1.13.3** | **🎨 Convention (fond rouge) vs horaire modifié (écriture rouge) — RASTÉRISATION du PDF, pas le suffixe (Kevin 2026-06-29 captures + légende)**. Diag pixel sur le PDF réel (lesson #115) : le suffixe `'`/`"` N'EST PAS un marqueur de convention (apostrophe-only codes = 2191 fond BLANC / encre noire = travail normal). La vérité = la couleur RENDUE de la cellule : **fond ROUGE = convention** (jour de R travaillé, « jour en plus », ex `19/3c` jour 5 de Kevin) ; **encre rouge / fond blanc = horaire modifié** (pas un repos, ex `16/3c` jour 8). L'ordre des opérateurs PDF ne capture PAS ce rouge → `_doExtractPdfLines` RASTÉRISE chaque page (canvas, non-fatal) et échantillonne fond+encre par cellule → marqueurs `{{CONV}}`/`{{MODH}}`. Les parsers grille strippent les marqueurs (sinon décalage/perte — régression attrapée par le cross-check) + filet `_cmcApplyLeakedColorMarkers`. Porté par la **meta** (`bg:"CONV"` / `fg:"red"`), JAMAIS par mutation du code (reproduction à l'identique : `19/3c` reste `19/3c`). Rendu app (`vPlan`) + page Départs + seed/boards (régénérés). MODH sur code apostrophe : la meta `fg:red` force fond blanc (infer ne met plus CONV si fg=red). Vérifié pixel (DESARZENS Août : j1/j3 `'c` blanc, j5 `19/3c` rouge, j8 `16/3c` écriture rouge) + visuel app+page + `test:conv-colors` (5/5) câblé `test:ci`. `test:ci` EXIT 0 (fidélité + cross-check + couverture intacts). ⏭ Reste : codes apostrophe NON-chef encore rougis par l'heuristique pré-existante (`cmcCodeStatusBg`/`codeStyle` `/['"]$/`) + couleurs des en-têtes de dates (weekend/affluence) — passe « tout à l'identique » suivante. |
| **v9.829** | **🎨 Couleurs « à l'identique comme l'import » + DERI W/DELTTRA (Kevin 2026-06-29, captures + PDFs)**. (1) **Couleurs** : le PDF SBM colore les cellules par STATUT (cellule de TRAVAIL = BLANC ; congé = rose, RH = violet, R = lavande, Convention `'`/`"` = rouge, CDP `*` = orange, AF = vert, M = rouge clair, RRT = jaune), pas une couleur arc-en-ciel par horaire. Nouveau `cmcCodeStatusBg(code)` (app `vPlan`) + `codeStyle` réécrit (page Départs) → les deux surfaces reproduisent exactement l'import (vérifié par capture : 14/19c/20/5c blanc, RH violet, 16/3* orange). **Convention** = jour de repos R TRAVAILLÉ (jour en plus) = suffixe `'`/`"` **n'importe où** (même `19/4'c` chef) → ROUGE (Kevin « manque le rouge pour les jours de convention »). (2) **DERI W** : sa cellule jour-1 « DELTTRA » (code opaque du PDF) était droppée → remplacée par un faux RH → DERI séparé dans une fausse équipe (r6) → seul vrai « à vérifier » de v9.828. FIX (lesson #69) : « DELTTRA » enregistré comme code de travail reconnu (CODES + CODE_RE + EXCLUDE_NAMES) → préservé À L'IDENTIQUE en jour-1, ne décale plus la ligne → DERI rejoint son équipe r3 (BOURDIER C/SEGGIARO G). Juillet « à vérifier » 1 → 0. `test:ci` EXIT 0 (reproduction 0 écart, cross-check app↔page OK). |
| **v9.828** | **🎯 Équipes « à vérifier » — fin des fausses alertes (Kevin 2026-06-29 « Tente sans régression »)**. Investigation (lesson #83 : prouver avant d'accuser l'algo) : sur Août+Juillet, **1 SEUL** vrai mauvais regroupement (DERI W, Juillet r6) ; les 26 autres équipes « à vérifier » étaient des **faux positifs** — la métrique `restBad` comptait les **CP-partiels** (présents une partie du mois → moins de jours de repos, mais TOUS inclus dans le pattern de l'équipe) comme « rotations distinctes ». Le clustering est sain ; c'est la VÉRIF qui criait au loup. FIX **diagnostic uniquement** (0 touche au clustering) : `vVerify` ne flague qu'un membre dont les repos NE SONT PAS un **sous-ensemble** du pattern dominant (vraie rotation ≠ mal fusionnée) + un singleton ; une grande équipe à repos homogènes est légitime (règle Kevin : mêmes repos = même équipe). Mesuré : Août **12→0**, Juillet **15→1** (DERI W honnêtement signalé). Équipe de Kevin (Éq.11/13) + 28 miroirs **inchangés**, clustering identique (40/37 équipes), reproduction 0 écart. **Harness régen déterministe** (lesson #88) : `_gen-boards`/`_gen-seed` poll jusqu'à couverture STABLE (la passe géométrique différée est sensible à la contention CPU → couverture variait 247↔289) ; boards+seed régénérés à l'état pleinement résolu. `test:ci` EXIT 0 (everyone-has-planning, kevin-truth, garro-cp, team-rule, vplan, verify, cross-check tous verts). |
| **v9.827** | **🔐 Face ID niveau B dans CMCteams (Kevin 2026-06-29 « Oui pour FaceID dans CMCteams »)**. CMCteams avait déjà tout le WebAuthn (register/login/auto-prompt sur l'écran de login). Le manque : après la 1ʳᵉ connexion par CODE, l'employé n'était jamais **invité** à activer Face ID (enrôlement enterré dans le profil) → il retapait son PIN à vie. FIX : `_cmcOfferFaceIdEnroll(uid)` appelé à la fin des 3 chemins de login par code → modale 1×/compte+appareil « Activer Face ID ? » (Activer / Plus tard / Non merci). **Fail-open total** : non supporté / refus → le code reste la méthode (jamais de lockout, règle #55/#81). « Plus tard » = re-proposé au prochain login ; « Non merci » = flag `cmc_faceid_offered_<uid>=no` (plus jamais). Test `test:faceid` (10/10) câblé `test:ci`. → 1ʳᵉ connexion = code, ensuite Face ID auto. |
| **v9.826** | **🎨 Nomenclature lieux par couleurs** : `getCodeLieu` → code normal = CMC (défaut), marque CDP (suffixe `*` / flag cdp, orange PDF) = Café de Paris, repos/absence = null, cadres = `CADRE_LIEU` prioritaire. Carte vVerify « Lieux » : détail par lieu (ex « 31 CMC · 3 Café de Paris ») au lieu de « sans lieu mappé ». Mesuré Août : 34/34 codes mappés. |
| **v9.825** | **🤒 Maladie longue durée (PORASSO C / ROBIN M / CAPRA C) — fin du « problème » de couverture (Kevin 2026-06-28 « Les 3 employés sont en maladie. Longue durée »)**. Ces 3 apparaissent dans le PDF (poste+nom+matricule+période `1 30`/`1 31`) SANS code journalier → ROBIN M/CAPRA C pas même créés, PORASSO C 0 cellule → flaggés « problème bloquant » de couverture (ce ne sont PAS des échecs parser : maladies longue durée listées sans grille). FIX : `_cmcApplyMaladieLongueDuree(iy,im)` (liste métier éditable `cmc_maladie_ld`, défaut = les 3) appelée EN DERNIER dans `_postValidateImport` + `_cmcFinalGeometricFill` : si le nom est présent dans le TEXTE SOURCE du mois (jamais d'invention pour un absent du PDF), find-or-create + remplit les jours VIDES de « M » (ne masque aucun code réel — règle #1) + `teamHistory[key]="maladie"`. Mesuré (import réel Août) : les 3 = 31 cellules M, équipe Maladie → vVerify « **Pas de problème bloquant** ». Seed + boards régénérés → les 3 dans la section 🤒 Maladie de l'app ET de la page Départs. `test:ci` EXIT 0. |
| **v9.824** | **🌱 Seed planning partagé app↔page (option B Kevin « intègre toi-même les nouveaux plannings dans l'app et la page »)** : `tools/shared/planning-seed.js` (généré depuis les VRAIS PDF, même source que la page Départs) chargé par l'app et appliqué APRÈS la restauration de session + ré-appliqué après `fbApplyData(cmc_ov)`. Idempotent : remplit UNIQUEMENT les mois vides (hasLive guard), n'écrase jamais le live, aucun push Firebase ; désactivable via `window.__CMC_NO_SEED`. → quand j'intègre un mois, il apparaît dans CMCteams ET la page sans action Kevin. `tools/shared/_gen-seed.mjs` régénère le seed. `test:seed` (8/8) câblé `test:ci`. |
| **v9.823** | **🩹 Kevin/chefs sans équipe + 291 jours hors-limite (diag v9.822 Août/Juillet)**. (1) DESARZENS K = « (aucune) » en Août ET Juillet : les chefs (2-3/section, restKey légèrement différent à cause d'un CP/ajustement) ne clusterisent pas par restKey EXACT et le fallback `family:offset` ratait → **FILET FINAL** dans `_cmcFallbackUnassignedTeams` : tout emp qui TRAVAILLE (≥5 j travaillés, ≥2 repos) encore sans équipe est rattaché à l'équipe de TRAVAIL de sa famille dont les repos chevauchent le plus les siens (Jaccard ≥0.5). Garde-fou lesson #112 : seuil ≥5 j → GARRO (3 j) reste exclu, jamais collé de force. `teamHistory` = regroupement d'affichage (jamais une cellule PDF) → ne viole pas « reproduction à l'identique ». Mesuré : Juillet = équipe EXACTE de Kevin (PUGNETTI/DESARZENS/MARIOTTINI/DESSI/MALENFANT/DEJANOVIC). (2) « 291 cellules jours hors limite » (Juillet device) : l'import est déjà à 0 (filet v9.801) ; les oob venaient de données cloud STALE ré-injectées APRÈS le sweep boot one-shot v9.809 (race fbLoadAll) → **STRIP oob à CHAQUE application Firebase** (`fbApplyData` cmc_ov) + re-push correctif une fois + cap défensif dans la passe géométrique différée `_cmcFinalGeometricFill`. Mesuré import Août/Juillet oob=0, couverture 282/290 inchangée. vVerify continue de signaler honnêtement les noms en `{{REDNAME}}`/sections fragmentées sans grille (PORASSO C/ROBIN M/CAPRA C — vrai positif, jamais fabriqué). `test:ci` EXIT 0 (vplan 9/9, verify 18/18). |
| **v9.788** | **🔐 Chantier 2 Phase A — Auth Firebase activable (canary par device)**. Le plumbing auth anonyme existait déjà (`cmcFbAnonAuth`→`_fbAuthToken`→`_fbAuthQS()` threadé dans fbWrite/SSE/fbLoadAll, fail-open : sans clé = comportement actuel). Ajouté : **champ admin « 🔐 Auth Firebase »** (bandeau config) pour coller la **clé Web API publique** → `cmcSaveFbApiKey()` stocke `cmc_fb_apikey` (PAR DEVICE) + (ré)init + toast état. Mécanisme **canary** : Kevin colle la clé sur SON device → seul son device s'authentifie (token `?auth=` attaché) → vérifie que la sync marche → puis activation globale + durcissement règles `/cmcteams` (Phase B/C). **Fail-open total** : clé vide/refusée/réseau KO → app fonctionne comme avant (règles ouvertes). Test fonctionnel réel `test:v788` (8/8 : gating `_fbAuthQS`, no-op sans clé, token attaché avec clé+réponse mockée, persistance). `test:ci` **29 suites/0 FAIL** EXIT 0. Worker `/health` confirmé vivant par Kevin. Isolation CMCteams. ⏭️ Reste (Kevin) : canary device → durcir `/cmcteams .read:"auth!=null"` dans le fichier PARTAGÉ (sous-bloc seulement) + publier console (rollback armé). |
| **v9.787** | **🔒 FUITE SECRET CORRIGÉE (vraie P0, pas du théâtre)** — Chantier sécu exécuté. `_adminCfgBackup()` poussait la **clé Anthropic EN CLAIR** vers Firebase `/cmc_admin_cfg.json` alors que la **DB est ouverte** → n'importe qui avec l'URL lisait le secret. FIX : `iaKey` retiré du backup Firebase (la clé reste device-local `FB_LOCAL` + fallback cross-app Apex `ax_shared_api_key`) + **migration/scrub auto** au restore (récupère la clé léguée en local UNE fois puis re-PUT sans `iaKey` → efface la clé de la DB ouverte). Choisi vs chiffrer le localStorage local (= théâtre : clé de déchiffrement sur le même device ; lesson #55 XOR sync banni). Test fonctionnel réel `test:v787` (Playwright 4/4 : intercepte le PUT, prouve l'absence de `iaKey`/secret) câblé `test:ci` → **28 suites PASS / 0 FAIL**, EXIT 0. Isolation CMCteams. NB : `cmc_pw` (hashs salés 10k) reste synchronisé sur la DB ouverte → fermé par le Chantier 2 (Firebase Auth+rules). |
| **v9.786** | **🔒 Durcissement sécurité (re-audit mesuré, claims vérifiés)**. Un 2e audit crew annonçait 2,5/5 — **vérifié et largement erroné** : rate-limit PIN EXISTE (`cmc_pin_fails` verrouillage progressif L16082), session TTL 8h EXISTE (L9093+auto-logout L9122), CSP EXISTE et restrictive (`default-src 'self'`, `object-src 'none'`, `form-action 'none'`, allowlists connect/img-src). Le `'unsafe-inline'` script-src est **inhérent** au SPA sans build (47k lignes de handlers inline) — non retirable sans casser l'app. Vrai score ≈ **4/5**. Corrigés (gains sûrs et réels) : (1) lien externe APEX `target="_blank"` → ajout `rel="noopener noreferrer"` (anti-tabnabbing ; les `window.open("",…)` blanches même-origine laissées car elles gardent la référence — noopener les casserait) ; (2) stack traces de l'écran d'erreur `render()` **réservées à l'admin** (anti information-disclosure pour les employés). NON faits car architecturaux/risqués (documenté honnête, pas de faux 5/5) : migration CSP nonce (impossible sans build), vault clé IA, enforcement Firebase rules server-side. `test:ci` 27 suites PASS/0 FAIL, EXIT 0. Isolation CMCteams. |
| **v9.785** | **🏛 Architecture → zéro route morte (vers le 100/100 par axe, mesuré)**. Les 3 dernières routes brisées (audit crew « 4 routes pointant vers fonctions non définies ») sont câblées sur de VRAIES vues, sans toucher au `switch` (les `case` testaient déjà `typeof vXXX==="function"` → définir les fonctions les allume) : (1) **`vCrossTeamActivity()`** = NOUVELLE vraie vue admin « 📊 Activité cross-team » (stats réelles depuis `cmc_chat` : KPI 24h/7j/30j + publics/privés cliquables, tableau par équipe, top contributeurs avec barres) ; (2) **`vParserIntelligence()`** = alias `vParserLearning()` (vue réelle existante) ; (3) **`vParserCompare()`** = alias `vImportVersions()` (compare des versions d'import). Combiné à v9.784 (pitmap→`vMapEditor`) : **0 route morte restante**. **UX** : 2 boutons interactifs 36px→44px (Apple HIG, chat search + suppression page scan). Test fonctionnel réel `test:v785` (Playwright 7/7) câblé `test:ci` → **27 suites PASS / 0 FAIL**, EXIT 0, fidélité 29/29. Isolation CMCteams (index.html/sw.js/tests). |
| **v9.784** | **🔧 3 chantiers post-crew (vérifiés un par un, isolation CMCteams confirmée — rien d'autres projets)**. Suite à l'ultra-review : le crew avait sur-rapporté. Vérité mesurée : (a) **scroll auto #758 DÉJÀ corrigé** (v9.758, `dc()` ne re-scroll qu'au `_viewChanged`) ; (b) **intervalles « zombies » = FAUX positif** (les `setInterval` top-level/IIFE sont des singletons créés 1× au chargement, pas de fuite ; celui re-créé dans une vue a déjà son `clearInterval` guard) ; (c) **IDB restore DÉJÀ en place** (`_cmcAgentPersistenceWatch` restaure `CMC_CRITICAL_KEYS` depuis le shadow IDB) — le crew sécu s'était trompé. **Vrais correctifs livrés** : (1) **route `pitmap`/`pitmapview` câblée sur la vraie carte `vMapEditor()`** pour l'admin (le bouton « Tables Live (carte) » ne tombe plus sur un stub « en développement »), fallback non-admin conservé ; (2) **`render()` ne scroll la grille qu'en vue planning** (cohérent avec `dc()`, supprime un `adjGrid` inutile à chaque rebuild complet). **Test fonctionnel réel** `test:v784` (Playwright, 5/5) : routes admin/non-admin + preuve de la détente v9.783 Règle 2 (CP intercalée ne crée plus de faux « max_jours_consec », ET vrai bloc 8j toujours détecté). Câblé dans `test:ci` → **26 suites PASS / 0 FAIL**, EXIT 0. Messages d'erreur : conservés (contextes admin = diagnostic voulu, règle « détailler erreurs »). |
| **v9.783** | **🧘 DÉTENTE vérifications (ultra-review crew 4 agents : vérif/détente, archi, sécu, UX)**. Assouplissement ciblé des contrôles d'import trop stricts (faux positifs SBM), SANS toucher au filet absolu « import lossless / tout le monde a un planning » ni au seuil coverage<0.5. 5 corrections dans `detectRepoConflicts` + scoring : (1) **Règle 1 repos_insuffisant** : seuil PROPORTIONNEL aux jours de présence (travail+repos) au lieu du calendrier → emps en sous-période 1-15/16-31 ou bloc CP intra-mois plus faussement signalés. (2) **Règle 2 max_jours_consec (vrai bug)** : TOUT code non-travail (R/RH **+ absences CP/AF/M + vide**) brise la série — avant, une absence « pontait » 2 blocs de travail = faux positif « 7j consécutifs ». (3) **Règle 3 donnees_manquantes** : seuil 50%→60% (sections PDF fragmentées Roulettes/CMC). (4) **`_cmcDetectIdenticalScheduleBug`** : seuil cadres 60%→75% (rotations chef partagées légitimes, Convention art.11 ; déjà banner non-bloquant). (5) **`_cmcMaybeAutoVisionFallback`** : auto-Vision + toasts « joins image » SEULEMENT si score<70 (severity critical) au lieu de <90 → fin du spam d'escalade Apex sur imports 70-89% OK. Conservé inchangé : coverage<0.5 banner régression, garantie nom-dans-PDF⇒cellule, reproduction fidèle, pas d'auto-rollback (v9.641). `test:ci` exit 0 (25 suites PASS/0 FAIL), fidélité 29/29. Audits crew : sécu 4/5, archi 7.2/10, UX/fluidité 6/10 (à suivre : intervalles zombies + scroll auto #758). |
| **v9.768** | **🎯 COURTIN F / COTTALORDA D — réassignation full-absence (Kevin diag JUIN « Toujours pas bon » : encore dans BJ Éq.1 malgré v9.764)**. Cause restante après v9.766 : le parser géométrique remplit désormais les cellules CP de COURTIN F (20 CP + 5 RH + 5 R). v9.744 absence-team voyait ratio CP = 20/30 = **67 %** < seuil 80 % → ne réassignait pas à « conges » → early-return sur `teamHistory[key]` déjà posé par pdf-column → restait collé à BJ Éq.1. v9.764 purge ne tirait pas non plus (pdf-column avait déjà fixé teamHistory). **FIX** : (a) ratio sur **JOURS NON-REPOS uniquement** (exclut RH/R du dénominateur) → COURTIN F = 20/20 = 100 % CP → qualifie « conges » ; (b) v9.744 **OVERRIDE** un teamHistory de TRAVAIL existant quand l'emp qualifie pour une équipe d'absence ; (c) nouvelle équipe « ✈ En déplacement » pour DEPL/DEP intégral (rendue dans vPlan section spéciale) ; (d) `amenage` ajouté au whitelist v9.764. Test `test:v768` (8/8) câblé `test:ci`. Suite : `test:ci` exit 0, fidélité 29/29, homonymes 3/3, geometric 12/12. COURTIN F/COTTALORDA D doivent désormais apparaître dans « 🏖️ Congés » au lieu de BJ Éq.1. |
| **v9.767** | **🧹 UX import épurée (Kevin « efface le copier-coller et le reste, j'importe un dossier à chaque fois »)**. Retrait des sections inutiles de `vImport` : (a) zone de copier-coller (textarea visible + boutons Analyser/Appliquer/Coller header + warning Pit Boss), (b) carte « Joindre image visuelle du planning », (c) carte « Scanner planning camera iPhone ». Kevin importe TOUJOURS un fichier PDF → `handleFileImport` auto-déclenche `doImport` après extraction → boutons Analyser/Appliquer redondants. `impTxt` conservé en BUFFER caché (data-only, `display:none`) car `doImport` + `handleFileImport` le lisent/écrivent en interne. UI nouvelle : sélecteur mois/année + carte fichier PDF (drag-drop / choisir) + carte compacte (Historique V1/V2/V3 + Export source diag + repli « Outils avancés » avec Tests parser, Re-tenter cadres, OCR Vision, diagnostics, wipe cadres) + zones résultat (impRes/impRollback/impAutoVerify). -5578 octets net. `test:ci` exit 0 (fidélité 29/29, homonymes 3/3, geometric 12/12, RUNTIME 100/100). |
| **v9.766** | **🎯 PARSER GÉOMÉTRIQUE T1 VALIDÉ INTÉGRÉ EN PROD — Roulettes/CMC fragmentées + encadrés M/CP/FORMATION (Kevin « intègre tout comme V1, il ne restait que les couleurs »)**. L'ancien `_extractPdfLines` capturait les coordonnées x/y mais reconstruisait par LIGNE (y) puis aplatissait → cassait sur les sections où les noms sont en bloc d'en-tête séparés des lignes de codes (Roulettes/CMC) et sur les encadrés top. La lib T1 validée (`window.EncadresParser`, `TextParser.parseFromPdfJs`, `CodeColors`) reconstruit par COLONNE (clustering x-bands) et a été testée 100 % sur le vrai PDF juin de Kevin. FIX : 3 libs UMD inlinées verbatim + collecte géométrique `window._cmcPdfGeometry={pages:[{items:[{str,x,y,w,h}]}]}` dans `_doExtractPdfLines` (additif, garde le chemin texte existant) + bridge `_cmcApplyGeometricParse(iy,im)` qui match par nom exact (homonyme-safe via initiale) et REMPLIT UNIQUEMENT les cellules vides (`cells_overwritten_blocked` = garde-fou reproduction identique). Câblé dans `_postValidateImport` AVANT `_cmcApplyValidatedTeams` → détecteur d'équipes voit des données complètes → meilleur miroir + sections. Mesuré sur capture réelle JUIN_2026_V1 : **6478 cellules grille + 990 cellules encadrés** remplies (238/276 emps géométriques matchés ; 30 collisions bloquées). Nouveau test `test:geometric` (12/12) câblé dans `test:ci`. Suite verte : fidélité 29/29 inchangée, homonymes 3/3, Pit Boss 20/20, validated-teams 10/10, RUNTIME 100/100. ⚠ Pour bénéficier de la lecture par colonnes : importer le **fichier PDF** (drag-drop / choisir), pas un copier-coller (qui perd la géométrie). |
| **v9.765** | **🧹 Garde DISPLAY vPlan : un emp 0-jour-travaillé « · RH R » n'apparaît plus dans une équipe de TRAVAIL (Kevin diag : COURTIN F/NICASTRO/COTTALORDA TOUJOURS dans Éq.1 malgré v9.764)**. v9.764 purge `teamHistory` à l'IMPORT, mais les données héritées (import précédent) gardaient `teamHistory=1` → toujours affichés. FIX au niveau AFFICHAGE (marche sans re-import) : `_planHasWork(e)` dans vPlan — un emp visible dans une équipe de travail (1-8/r*/c*) SEULEMENT si (a) ≥1 jour travaillé OU (b) absence INTÉGRALE du mois (CP/M/AF plein mois = vraie donnée, règle #1). Masqué uniquement si 0 travaillé ET ligne INCOMPLÈTE (codes non parsés = section fragmentée). Appliqué à « Ma section » + boucle familles ; les sections d'absence (formation/maladie/conges/amenage) rendues séparément NE passent PAS le filtre → restent visibles. Vérifié (emps réels actifs) : FAUTRIER/DESARZENS visibles, LANDAU B en CP intégral visible, GATTI B « · RH R » masqué. Test `test:v702-avril` mis à jour (marqueur strict `if(th!==tid)return false`). `test:ci` exit 0. Root cause restant = parser Roulettes/CMC fragmentées (lesson #66). |
| **v9.764** | **🧹 GARDE FINALE invariant : 0 jour travaillé ⇒ pas d'équipe de travail (Kevin diag : COURTIN F/NICASTRO/COTTALORDA TOUJOURS en BJ Éq.1 malgré v9.763)**. Le gate v9.763 dans `_cmcFallbackUnassignedTeams` ne suffisait pas : ces emps recevaient `teamHistory=1` d'une AUTRE passe (parser/pdf-column/DEF_EMP), pas du fallback. FIX : passe FINALE à la fin de `_cmcFallbackUnassignedTeams` (dernière passe dans les 3 flux import/boot/standalone) qui PURGE `teamHistory[key]` de tout emp actif avec <5 jours travaillés s'il est dans une équipe de TRAVAIL (1-8/r*/c*). EXCEPTION conservée : équipes d'absence `formation`/`maladie`/`conges`/`deplacement` (v9.744 — Kevin veut ces familles). Vérifié : COURTIN F (0 travaillé, Éq.1) → purgé « (aucune) » ; vrai travailleur gardé ; TESTA G (maladie intégral) → équipe `maladie` conservée. `test:ci` exit 0 (couverture 92.5→88%, attendu : ~24 emps 0-travaillé retirés ; ≥82% baseline OK ; fidélité 29/29, homonymes 3/3). Root cause restant = parser sections Roulettes/CMC fragmentées (lesson #66) pour donner leurs VRAIS codes à COURTIN & co. |
| **v9.763** | **🧹 Emps « · RH R » (0 jour travaillé) ne polluent plus les équipes (Kevin diag juin : COURTIN F/NICASTRO/COTTALORDA dans BJ Éq.1)**. Le fallback `_cmcFallbackUnassignedTeams` (v9.743) exigeait `cells≥10` mais comptait RH/R comme cellules → un emp en « · RH R » (5 RH + 5 R = 10 cellules, **0 travaillé**, codes travail non parsés car section Roulettes/CMC fragmentée) passait le seuil et était collé à la plus grosse équipe de sa famille+offset → polluait l'équipe de Kevin. FIX : nouveau `_workedCount()` (compte les jours hors RH/R/absence) + gate `<5 jours travaillés → pas d'équipe` (même seuil que la lib validée `minWorkDays:5`). Un tel emp s'affiche « sans équipe » (honnête) au lieu d'une fausse équipe. Vérifié : emp 0-travaillé → équipe « (aucune) ». `test:ci` exit 0 (fidélité 29/29, couverture stable). Root cause restant = parser les sections Roulettes/CMC fragmentées (lesson #66) pour que COURTIN F ait ses VRAIS codes. |
| **v9.762** | **🎯 PARSER VALIDÉ T1 INTÉGRÉ EN PROD — détecteur équipes/miroir (Kevin « tout marchait dans le bac à sable, pas dans l'app »)**. Cause racine de la frustration : le parser VALIDÉ (T1 `tools/planning-parser-tester/lib/`, testé 175 checks + fidélité 100%, équipe juin de Kevin 6/6) n'avait JAMAIS été branché dans l'app de prod — `index.html` tournait encore sur l'ancien détecteur buggé. FIX : `lib/team-detector.js` (UMD, `window.TeamDetector`) inliné verbatim dans `index.html` + bridge `_cmcApplyValidatedTeams(iy,im)` (construit l'entrée depuis `A.overrides`, mappe les ids génériques→ids prod par famille bj/r/c/amenage, écrit `teamHistory` + miroir `cmc_team_mirror_<key>`, ne touche JAMAIS les codes cellules) + câblé dans `_postValidateImport` comme source de vérité (rest-pattern reste en filet skip-on-explicit). Corrige les 2 bugs Kevin : (1) **RH ET R** comptés comme repos (avant : que RH) ; (2) **vrai miroir** = mêmes repos + horaires différents (avant : codé en dur/faux). Nouveau test `test:validated-teams` (10/10) câblé `test:ci`. Combiné avec v9.759-v9.761 : `test:ci` exit 0 (fidélité 29/29, homonymes 3/3, Pit Boss 20/20, RUNTIME 100/100, couverture 92.5%). Voir leçon #77. |
| **v9.761** | **🩹 « Il me parle de cadre ?! » + « l'app saute énormément sur l'import » (Kevin V2 juin)**. Sur un import chefs/employés (le planning cadres est un 2ᵉ PDF séparé), les 21 cadres DEF_EMP sans horaire étaient flaggés par 2 agents (`horaires` l.12416 + `import-watch` l.14558) qui posaient une action `auto:true` → `agentActionAutoFillCadres()` se déclenchait TOUT SEUL → toast « ✨ Cadres auto-complétés » (« il me parle de cadre ») + `dc()` (rebuild complet = scintillement) à CHAQUE import. Viole aussi lesson #48 (autoFill cadres = invention, jamais auto). FIX : retrait des 2 `auto:true` → l'auto-fill cadres devient MANUEL (bouton admin). Mesuré sur import V2 : `dc()` pendant import **4+ → 1**, **0 toast cadre**. Test régression `test:import-flicker` câblé dans `test:ci`. Aucune régression (fidélité 29/29, homonymes 3/3, RUNTIME 100/100). NB : la couverture d'équipes faible sur V2 complet (sections Roulettes/CMC fragmentées) reste le chantier suivant (lesson #66 géométrie). |
| **v9.760** | **🎯 V2 JUIN — maladie/formation/déplacement OK + homonyme distinct créé (Kevin « il manque les familles, malades, formation »)**. Diag sur le vrai planning V2 juin collé par Kevin. Confirmé : les codes statut **M (maladie), AF (formation), DEPL (déplacement), CP partiel, PAT** SONT reconnus et appliqués correctement sur les lignes chefs inline (TESTA G = M tout le mois, FAUTRIER j1=M, DESARZENS CP j1-15, VOUKASSOVITCH/DE RYCKE = AF+DEPL). **BUG trouvé et corrigé** : `LAVAGNA J` (absent) était IGNORÉ car nom de famille partagé (`LAVAGNA E`/`LAVAGNA Y` existent) + initiale `J` non résolue → branche « AMBIGU homonymes » faisait `continue` AVANT la création inline v9.759 → planning perdu (viole règle #1). Or une initiale qui ne matche AUCUN homonyme = **personne distincte** (lessons #38/#68, souvent des frères). FIX : retrait du `continue` → l'AMBIGU tombe vers la création inline (LAVAGNA J créé distinct, SANS jamais fusionner vers E/Y). Test régression `test:v2-codes` (M/AF/DEPL/CP + homonyme distinct) câblé dans `test:ci`. Suite verte : homonymes 3/3 (aucune régression), fidélité 29/29, Pit Boss 20/20, couverture 92.5%. Voir leçon #76. (Reste : sections fragmentées Roulettes/CMC + encadrés M/CP géométriques — à traiter sur le texte V2 complet quand Kevin teste.) |
| **v9.759** | **🎯 FAUTRIER « pas d'horaire » + règle #1 « tout le monde a un planning » (Kevin diag JUIN 2026-05-29)**. Deux causes racines. (1) **Séparateur mixte** : `sep` est GLOBAL (`sep="\t"` dès qu'UNE ligne a un tab), mais le fallback de reconstruction par espaces (l.~38922) ne tournait QUE si `sep===","`. PDF.js produit du MIXTE (sections en colonnes tab + lignes chefs space-séparées) → `BRTP+K 5 FAUTRIER M 1 30 …` splittée sur `\t` restait en 1 cellule → nom jamais matché → 0 code. FIX : le fallback tourne pour TOUTE ligne qui split en <4 colonnes (vraies lignes tab ≥4 → ignoré, 0 régression) + détection du **numéro d'équipe isolé** entre code-poste et nom (format v9.639 POST TEAM NAME FROM TO). FAUTRIER mesuré : 0 → **30 cellules** (j1=20/5c, j5=RH). (2) **Noms absents du roster** (DEF_EMP incomplet, ~37 noms) : au lieu de `INTROUVABLE (skipped)` qui violait la règle #1, **création INLINE** pendant la passe de parsing (codes appliqués MÊME passe → pas de ré-import fragile, pas de bug « 0 cellule »), gardée par `_cmcIsValidEmpName` (regex « SURNAME + INITIALE », rejette fragments/codes/« Colonne »). DEJANOVIC mesuré : créé `U_TMP_*` + 30 cellules. Bouton « Créer tous » fragile SUPPRIMÉ. Test régression `test:space-format` câblé dans `test:ci`. Suite : fidélité 29/29, homonymes 3/3, Pit Boss 20/20, couverture 92.5%, everyone-has-planning **279/0** (+27). Voir leçon #75. |
| **v9.758** | **🩹 SLIDE/SCROLL — le défilement horizontal revenait au jour du jour (Kevin 2026-05-29 « le slide ne fonctionne pas, ça revient au jour du jour »)**. `dc()` rappelait `adjGrid`/`adjDeparts`/`adjMonPlanning` (scroll auto vers aujourd'hui) à CHAQUE re-render (SSE/agents/flicker) → le défilement manuel de l'utilisateur était annulé en permanence (snap au jour du jour). FIX : ne re-scroller qu'au **changement de vue ou de mois** (`_viewChanged`), pas à chaque rendu. Le slide horizontal persiste maintenant. Réduit aussi le « saute ». `test:ci` exit 0. Note : le scintillement pur (flash du rebuild innerHTML) + le récap d'import effacé par un dc() tardif restent à régler par le rendu du récap via `vMain` (refactor à venir). |
| **v9.757** | **🎯 DÉTECTION ÉQUIPES — port de l'algo validé union-find (Kevin 2026-05-29, diag Juin)**. Le diag Juin montrait des équipes fausses : « équipe 1 » fusionnait deux rotations de repos (ton groupe RH=5,11,17,23,29 + un groupe RH=1,7,13,19,25 = équipe+miroir collés), FAUTRIER M exclu (9 cellules < seuil 10), DEJANOVIC D absent. Cause : l'algo prod groupait par signature grossière (cycle:offset:1er code) → fusionnait des rotations différentes. FIX : porté le détecteur **union-find par identité des codes journaliers** du banc d'essai T1 (validé 6/6 sur l'équipe juin de Kevin) → `_cmcDetectTeamsByDailyCodes(iy,im)`. Clé d'équipe = mêmes codes journaliers normalisés sur les jours travaillés en commun (≥6 co-présents, ≥3 travaillés, 100% match). Câblé AVANT `_cmcDetectTeamsByRestPattern` dans `dc()` + `_postValidateImport` (rest-pattern reste filet pour les non-clusterisés via skip-on-explicit). Mesuré (mai V1, boot réel) : **25/26 équipes à pattern de repos cohérent** (vs fusions multiples avant), `test:ci` exit 0 (fidélité 29/0, Pit Boss 20/0, homonymes 3/3, v719 12/0, tailles équipes 8/0, couverture 92,4 %). Validé sur MAI — à reconfirmer par Kevin sur son diag JUIN réel. |
| **v9.756** | **⏪ REVERT dcSoon v9.755 — régression « rien ne s'affiche à l'import » (Kevin 2026-05-28)**. Le `dcSoon()` (debounce 90 ms) de v9.755 DIFFÉRAIT le rendu principal de `doImport`. Or `doImport` fait `dc()` PUIS append impérativement le récap dans `#impRes` (`innerHTML +=`). En différant le `dc()`, le récap était ajouté à l'ANCIEN `#content`, puis le `dc()` différé reconstruisait `#content` → récap effacé. Mesuré (Playwright) : `#impRes` = **0** caractère sur v9.755 vs **986** sur v9.752 → panneau de résultat d'import VIDE. FIX : revert des 4 `dcSoon()` → `dc()` synchrone (restaure l'ordre rendu→append de v9.752, `#impRes`=986 vérifié). **Conservé** : `pageIn` en fondu d'opacité pur (sûr, CSS-only). Leçon #73. `test:ci` exit 0. Le scintillement import (4 `dc()` en rafale) reste à corriger par une méthode sûre (guard agent auto-fix pendant import) — sans différer le rendu. |
| **v9.755** | **🩹 ANTI-SCINTILLEMENT — rafale de rebuilds à l'import (Kevin 2026-05-28 « l'app saute scintille bcp trop partout mais surtout à l'import »)**. Diagnostic Playwright instrumenté (wrap `dc()` + stacks) : **4 `dc()` (rebuild `innerHTML` complet) en ~100 ms à chaque import** → 4 flashs plein écran. Sources : `doImport` (l.39588) + `applyAbsenceCadres` (l.25444) + l'agent auto-fix `autoFillMissingCadres`/`agentActionAutoFillCadres` qui se déclenche **en plein import** (double `dc()`). FIX : nouveau coalesceur `dcSoon()` (debounce trailing 90 ms) sur ces 4 sites → **4 rebuilds → 1** (mesuré). Distinct de `dcDebounced` (3000 ms pendant import, pour les échos SSE étalés). + `pageIn` passé en **fondu d'opacité pur** (retrait du `translateY(8px)` qui faisait « sauter » le contenu à chaque navigation). Display/perf only, `test:ci` exit 0 (chefs 29/0, Pit Boss 20/0, homonymes 3/3, couverture 92,4 %). |
| **v9.754** | **🎨 COULEURS « identiques à l'import » — cohérence grille ⇄ vue mensuelle (Kevin 2026-05-28 « Identique à l'import »)**. Incohérence inter-vues détectée : `vMonPlanning` rendait déjà `*`/CDP → **orange** et `R` → **lavande** (conforme NOTES_USER nomenclature PDF), mais la grille `vPlan`/`vDeparts` lisait la table `CODES` où `*`/CDP = pêche (#ffe4d0) et `R` = gris (#e8e8e8) → même code, couleur différente selon la vue. FIX : aligné `CODES` sur la vue mensuelle + le PDF documenté — `*`/CDP variants → orange (#ffc488), `R` → lavande (#d8c8f0). **Choix éditorial écarté** (pas de couleurs de lieu CCDP/POKER/CMC arbitraires du prototype T1) : Kevin = reproduction fidèle du PDF, pas de schéma inventé. Changement display-only (aucun impact données/fidélité). `test:ci` exit 0 inchangé (chefs 29/0, Pit Boss 20/0, homonymes 3/3, coverage 92.4%, everyone-has-planning 252/0). Convention (`'`/`"`) reste rouge/jaune (v9.727), AF vert, CP rose, RH violet. |
| **v9.753** | **🎯 FIDÉLITÉ CADRE — préservation casse `h` + suffixe `:` (intégration T1, audit `test:fidelity-pb` 1/19 → 20/20)**. Le parser grille jour-par-jour (l.39091) et `_applyCodeArr` (l.38219) faisaient `toUpperCase()` + strip → `12h30/19` stocké `12H30/19`, `19/4:` strippé `19/4` (18 nits cadre). FIX : GATE stricte `/h/.test(code)||/:$/.test(code)` AVANT normalisation → préserve la casse/suffixe + clone l'entrée `CODES` de base si absente (`learned:true` + `_rebuildCK()`). Les codes chefs/employés (`20/5c`, `22/6`, `RH`…) n'ont ni `h` minuscule ni `:` final → **0 impact, fidélité chefs 29/29 intacte** (vérifié). Le 19e nit (PENNACINO JP `21 31`) était un bug du TEST (`day=from+i`), pas du parser (v8.54 `fromDay=1` toujours) → test aligné `day=1+i`. Résultat : Pit Boss **20/20**, chefs **29/0**, homonymes 3/3, master 156/160, coverage 92.4%, everyone-has-planning 252/0 → `test:ci` exit 0, zéro régression. Voir erreur connue #71. |
| **v9.732** | **🛡 Test régression homonymes — `test:homonyms` câblé dans `test:ci`**. Filet anti-régression dédié aux 3 paires homonymes connues (CLAUDE.md #38/44 + NOTES_USER) : LANDAU B (chef U00062) ≠ LANDAU J (cadre P00003), ENZA B (U00070) ≠ ENZA C (P00009), CAMPI PH (U00045) ≠ CAMPI H (P00013). Le test injecte un fixture V1 synthétique (chefs+employés sans header cadre) via `doImport()` puis vérifie que les chefs reçoivent 28 jours et que les cadres reçoivent **0 jour** (gating `importFamily==="cadres"` v9.723 + `hasCadres` v9.728). Mesure : **3/3 paires correctement séparées**. Aucune modification du parser. `test:fidelity` mai V1 roulettes reste à **29/29** ; suite globale **156/160** (97.5%) inchangée. |
| **v9.731** | **📦 Merge PR #323 — bundle v9.726→v9.730**. Bump version + sw.js, regroupe : reproduction fidèle (v9.726), couleurs Convention (v9.727), cadres non contaminés (v9.728), fragmentation équipes (v9.729), section Horaires aménagés (v9.730). |
| **v9.730** | **🕐 Section « Horaires aménagés »**. Kevin : les 2 employés des sections aménagement (BLANZIERI K, ACCOMASSO F) finissaient en 2 équipes à 1 personne (`c13`/`c15`) car le PDF a des colonnes à count 0/1 dans ces sections. FIX : `_sectionFamily` détecte `AMENAGEMENT` (chefs OU cartes) → famille `amenage` ; `_cmcDetectTeamsByPdfColumn` regroupe tous ces emps dans UNE équipe `amenage` (« 🕐 Horaires aménagés », famille `cmc`) sans fragmentation par colonne ; `_cmcEnsureTeam` crée l'entrée dédiée. Vérifié : BLANZIERI K + ACCOMASSO F dans une seule section, 0 équipe cartes isolée à 1 personne. |
| **v9.729** | **🎯 Fin de la fragmentation des équipes — fusion lignes cassées PDF.js**. Kevin : « beaucoup d'équipe a 1-2 personne ». Cause : PDF.js fragmente une rangée d'employés en 2 lignes — ligne A = codes-poste + plages `1 31` SANS noms, ligne B = noms seuls (`MILLO W  HORGNE C  GARINO Y …`). `_extractEntries` attend `<poste> <nom>` adjacents → 0 entrée → colonnes sous-remplies → bloc jamais plein → équipes de 1-2 pers (équipe chefs BJ `2` = 2 au lieu de 5) ou ballonnées (`r3`=17). FIX : helper `_mergePosteNameLines()` dans `_cmcDetectTeamsByPdfColumn` ré-interleave les paires A+B (`posteN≥2 & nameN=0` suivi de `posteN=0 & nameN≥2`, comptes égaux) AVANT le parsing. Vérifié sur le **vrai texte PDF SBM mai 2026 V1 de Kevin** (`tests/fixtures/mai-2026-v1-full.txt`) : chefs BJ `1`-`6` = 4-5 emps, roulettes `r1`-`r12` = 3-6 (0 équipe ≤2, 0 équipe >8). Nouveau test `test:teamsizes` câblé dans `test:all`. |
| **v9.728** | **🛡️ Cadres non contaminés sur import V1 employés+chefs**. Sur un import V1 (employés+chefs, `hasCadres=false`), 2 scans de rattrapage cadres (le fallback v9.462 l. 39221 + le scan SECOURS brute-force v9.146 l. 39010) tournaient quand même et matchaient les cadres **par nom de famille seul** sur un homonyme employé → `CAMPI H` (cadre) recevait les codes de `CAMPI PH` (chef), `ENZA C` (Pit Boss) ceux de `ENZA B` (croupier). FIX : les deux scans sont gated sur `_importTypeDetails.hasCadres` — un import employés+chefs ne touche plus jamais les cadres (CLAUDE.md règle import incrémental). Vérifié : 0 cadre contaminé. |
| **v9.727** | **🎨 COULEURS CONVENTION — copie exacte du repère visuel PDF (Kevin 2026-05-21)**. Les codes à suffixe `'`/`"` = jours de Convention → dans le PDF SBM : **fond ROUGE, écriture JAUNE** (NOTES_USER « Nomenclature visuelle »). v9.726 clonait la couleur du code de base (`22/6'` rose comme `22/6`) → repère visuel perdu. FIX : (1) helper `_cmcEnsureQuoteVariant()` applique le style Convention (`bg #d8342e` / `c #ffe23a`) au lieu de cloner la base ; répare aussi les variantes déjà persistées ; (2) entrée `CODES["19/4'"]` corrigée en rouge/jaune ; (3) source unique `_cmcIsConv(code)` (`/['"]$/`) remplace les 8 `endsWith("'")` éparpillés — les codes à suffixe `"` (ex `19/4""`) étaient ignorés par la détection Convention (couleur cellule vMonPlanning, badge, méta `CONV`, compteurs). Résultat : `'` ET `"` colorés rouge identiquement dans toutes les vues (grille équipe, Mon Planning, Départs). Suite : **160/160 + test:fidelity 29/29 + test:all OK**. |
| **v9.726** | **🎯 REPRODUCTION À L'IDENTIQUE — 3 fixes parser import (audit `test:fidelity`)**. Nouvel audit `npm run test:fidelity` (Playwright) : importe le texte PDF source réel et vérifie que chaque cellule de `A.overrides` == le code source, caractère pour caractère. 3 écarts corrigés (cf. erreurs #62/#63) : (1) **décalage de jour** — la passe consensus d'équipe v8.57 écrasait HAREL/COSTAGLIOLI correctement parsés par une rotation miroir décalée → la passe ne remplit plus QUE les cellules vides ; (2) **`*` inventé** — 3 sites « auto-upgrade CDP » ajoutaient `16/3*` selon le profil `cdpShifts` → retirés (le `*` ne vient que d'une marque PDF réelle) ; (3) **suffixes `'`/`"` perdus** — helper `_cmcEnsureQuoteVariant()` clone l'entrée `CODES` de base dans la variante suffixée → `22/6'`/`19/4""` conservés exactement. Résultat : **29/29 employés reproduits à l'identique** (roulettes mai V1), suffixes préservés aussi en FORMAT B (pitboss). `test:fidelity` câblé dans `test:ci`. Suite : **160/160 runtime + test:all OK**. |
| **v9.725** | **🎯 FIX famille inversée — équipe BJ affichée sous "Jeux européens"**. Cause : `familyHistory[key]` (qui détermine la famille d'affichage BJ/Roulettes/CMC) a 2 écrivains : (1) le parser de grille (v9.655) qui pose `importFamily` depuis l'en-tête de section PDF — FIABLE ; (2) `_cmcDetectTeamsByPdfColumn` qui posait `fm` issu du curseur fragile des listes de noms (1ʳᵉ moitié du PDF, désaligné par les colonnes statut M/CP). Le pdf-column tournant APRÈS le parser de grille **écrasait** la famille fiable par la sienne, fausse → croupier BJ classé roulettes → équipe BJ sous "Jeux européens". FIX : pdf-column n'écrit `familyHistory[key]` que s'il est absent (`if(!emp.familyHistory[key])`) — la section PDF du parser de grille reste source de vérité ; fallback conservé pour l'isolation/edge-cases. Tests : **153/153 OK** (dont v719 12/12). |
| **v9.724** | **🧹 Purge totale historique (Kevin "Efface tout l'historique, vraiment partout")**. Le flag `cmc_v717_purge_done` était déjà posé sur le device de Kevin → le bloc v717 ne re-tournait plus. NEW flag `cmc_v724_purge_done` → `cmcWipeAllPlanningMemory()` re-déclenché une fois au boot : efface TOUS les mois `A.overrides`, `teamHistory`, `familyHistory`, snapshots `cmc_history_*`, shadow IndexedDB, nodes Firebase, logs, clés préfixées (`cmc_ref_*`, `cmc_team_mirror_*`, `cmc_import_src_*`, `cmc_auto_backup_*`…). Garde-fou v9.724 : le flag n'est posé QUE si le wipe a réellement tourné (admin présent) — sinon il ne se poserait jamais et le wipe serait sauté à jamais. App 100% vierge, re-import requis. Tests : **153/153 OK**. |
| **v9.723** | **🎯 FIX confusion homonymes — chef V1 collé sur fiche cadre (Kevin "LANDAU B chef → LANDAU J cadre")**. Cause : à l'import V1 (employés+chefs), un nom du PDF était matché sur un employé via 2 chemins sans contrôle d'initiale. (1) Fallback cadres `v9.126` : matchait `eName` à un employé `family==="cadres"` **par nom de famille seul** (`_surnameP===_surnameDef`), **sans vérifier l'initiale** ET **sans vérifier que l'import est un import cadres** → "LANDAU B" (chef BJ du PDF V1) collé sur la fiche Pit Boss "LANDAU J" → cadre avec horaire alors qu'aucun planning cadre n'a été importé. (2) Fallback bigram ≥0.85 : "LANDAU B" vs "LANDAU J" partagent 6/7 bigrammes → match accepté. FIX : (a) fallback cadres `if(!_fe && importFamily==="cadres")` + initiale obligatoire ; (b) fallback bigram refuse un match si surname identique mais initiale différente (LANDAU B≠J, ENZA B≠C, SEGGIARO G≠J — souvent des frères). Tests : **153/153 OK** (dont import E2E). |
| **v9.722** | **🎯 FIX lisibilité — nom d'équipe superposé aux dates dans la grille**. Kevin captures mai 2026 : dans les grilles d'équipe de Départs/Planning, le nom d'équipe ("Roul. Éq.4") s'affichait par-dessus les dates ("17 18 19") = illisible. Cause : la cellule-coin sticky de l'en-tête (`<th position:sticky;left:0>`) avait un fond `rgba(6,14,7,.22)` — 78% transparent. Au scroll horizontal de la grille, les cellules-dates glissent SOUS la cellule-coin et restaient visibles à travers. FIX : fond opaque `#0b1409` + `z-index:6` (> jours z-index:2) sur les 2 cellules-coin (vPlan `_renderTeamBlock` + vDeparts). Tests : **153/153 OK**. |
| **v9.721** | **🎯 FIX "Ma section" invisible — équipes détectées non enregistrées dans A.teams**. Cause racine (diag captures Kevin mai 2026) : `_cmcDetectTeamsByRestPattern` / `_cmcDetectTeamsByPdfColumn` écrivent `emp.teamHistory[key]=teamId` avec des IDs séquentiels ("7","11"…"30", "r9"…) qui ne sont PAS dans `DEF_TEAMS` → `gt(teamId)` retourne null → vPlan `utid=(_myTeamId&&gt(_myTeamId))?…` tombait à null → "Ma section" jamais affichée, miroir absent, nom d'équipe vide ("rien d'écrit"). FIX additif : (1) NEW helper `_cmcEnsureTeam(tid)` crée l'entrée A.teams manquante (nom dérivé du préfixe d'id : BJ/Roul./CMC Éq.N, couleur palette) — idempotent, ne modifie/supprime jamais l'existant. (2) vPlan : `utid=_myTeamId||null` + `_cmcEnsureTeam(utid)` — Ma section s'affiche dès qu'il y a teamHistory. (3) vDeparts : `myMirT` résolu via `_cmcMirrorTeam` même sans pré-enregistrement. (4) `_cmcMirrorTeam` utilise `_cmcEnsureTeam`. (5) Les 2 détections appellent `_cmcEnsureTeam(teamId)` + persistent `cmc_t`. Tests : **153/153 OK**. |
| **v9.720** | **🎯 AFFICHAGE — ma section (équipe + miroir) en premier, familles fermées par défaut**. Kevin "Je veux mon équipe en première vu ensuite mon miroir puis le reste en famille fermé par default. Pareil chez tout le monde individuellement". (1) **vPlan** : `_planFamOpen` toutes familles `false` au boot → tous les dossiers familles fermés, l'utilisateur déplie ce qu'il veut. La section "Ma section" (équipe + miroir) reste affichée en haut, hors familles. (2) **vDeparts** : `myTeam` basé sur `teamForMonth(A.user,…)` (équipe DU MOIS, jamais `emp.team` statique DEF_EMP) ; `myMirT` via nouveau helper `_cmcMirrorTeam(tid,y,m)` qui lit `cmc_team_mirror_<key>` puis fallback `gm()` ; `_depFamOpen` toutes familles `false`. (3) NEW helper `_cmcMirrorTeam` après `gm()` — résout le miroir du mois depuis la map d'import, fallback `gm()` legacy. Résultat : chaque user (Kevin, Laurence, chaque employé) voit SON équipe + SON miroir en tête, puis familles BJ/Roulettes/CMC fermées. Tests : **153/153 OK**. |
| **v9.719** | **🎯 REFONTE détection équipes par colonne PDF**. Kevin (AskUserQuestion) : "Refonte détection par colonne PDF". Nouveau `_cmcDetectTeamsByPdfColumn(srcText,iy,im)` (l. 34494) : parse les entêtes de rotation (`<count> <code> du au` répétés = N colonnes), remplit chaque colonne verticalement en sautant les colonnes fermées, ignore le débordement des encadrés statut, pose un garde `suspect` si `sumPlaced≠sumCount`. Écrit `teamHistory`/`familyHistory` + map miroir `cmc_team_mirror_<key>` (block1-colN ⇌ block2-colN). Chaque COLONNE PDF = une équipe distincte → plus de mélange de familles. Wiré dans `_postValidateImport` en hybride avec `_cmcDetectTeamsByRestPattern` (fallback jours de repos). Vérifié contre PDF réel : équipes conformes à la vérité terrain NOTES_USER.md. Tests : **153/153 OK** + suite dédiée `test:v719` (12 checks). |
| **v9.718** | **🎯 FIX parser entête aligné sur v9.715 (audit multi-agents)**. Audit : `_applyCodeArr` (parser blocs entête) était resté en v9.712 — son chef-clone `_ac.replace(/C+$/,"")` ratait les codes chef avec apostrophe (`14/19'c` → base `14/19'` absente de CODES → chef non détecté) et les codes déjà en minuscule. FIX : même regex `^(.+?)('?)C+$` que le parser grille v9.715 → base nue + apostrophe capturées séparément, `c` minuscule garanti. Tests : **141/141 OK**. |
| **v9.717** | **🧹 Purge totale historique (Kevin "efface tout l'historique partout")**. Les one-shots de wipe v703/v704 avaient déjà leur flag posé → ne re-tournaient plus. NEW flag `cmc_v717_purge_done` → `cmcWipeAllPlanningMemory()` re-déclenché une fois au boot : efface TOUS les mois `A.overrides`, `teamHistory`, `familyHistory`, snapshots `cmc_history_*`, shadow IndexedDB, nodes Firebase, logs. Repart 100% propre — re-import requis ensuite. Tests : **141/141 OK**. |
| **v9.716** | **🎯 FIX mémoire avril dans Départs**. Kevin "il a encore la mémoire d'avril dans l'onglet Départs". Cause : l'auto-cleanup des mois antérieurs (`_cmcAgentMonthCleanup` v9.704) se basait sur `A.month` (mois AFFICHÉ). Si Kevin navigue sur avril, `A.month=3` → avril jamais `< _curMo` → jamais effacé. FIX : (1) `_curMo` basé sur la DATE RÉELLE (`new Date()`) — tout mois avant le mois calendaire courant est effacé quelle que soit la navigation ; (2) l'auto-cleanup synchronise AUSSI le shadow IndexedDB (`cmcIdbSet cmc_ov/cmc_ov_meta/cmc_e`) — sinon l'IDB gardait l'ancien `cmc_ov` avec avril et `cmcRestoreFromAll` pouvait le ré-injecter. Tests : **141/141 OK**. |
| **v9.715** | **🎯 FIX apostrophes courbes parser grille (suite v9.714)**. Diag v9.714 : chefs avec code apostrophe (`14/19'c`) toujours mal groupés (`14/19'` ≠ `14/19`). Cause : le parser grille jour-par-jour ne normalisait PAS les apostrophes courbes U+2019 (contrairement à `_applyCodeArr`). La normalisation de signature `/[c'"*]/` (apostrophe DROITE) ne strippait pas le `'` courbe → `14/19'` distinct de `14/19` → chef isolé. FIX : (1) normaliser `[“”]→"` et `[‘’ʼ]→'` dans le parser grille ; (2) chef-clone robuste via regex `^(.+?)('?)C+$` → base nue (les bases `14/19'`/`16/3'` n'existent pas dans CODES, seules les nues) + `c` MINUSCULE garanti (`_famFromCells` `/c$/` est sensible à la casse). Tests : **141/141 OK**. |
| **v9.714** | **🎯 FIX parser grille jour-par-jour — strippait le `c` chef (suite v9.712/713)**. Diag v9.713 mai 2026 : chefs BJ (DESARZENS K, GATTI B, CIOCO S…) toujours détectés "roulettes". Cause : v9.712 avait corrigé `_applyCodeArr` (parser entête) mais le parser de la GRILLE jour-par-jour (l. ~38034, source réelle des cellules `A.overrides`) appliquait encore `replace(/[C"']+$/,"")` → `22/6C` → `22/6` → tous les codes chefs "plain" → `_famFromCells` ne voyait plus le `c` → "roulettes". FIX identique v9.712 appliqué à ce parser : préserver le `c`, cloner l'entrée `CODES` de base (`chef:true`). NEW flag `cmc_v714_redetect_done`. Tests : **141/141 OK**. |
| **v9.713** | **🎯 FIX détection famille + signature équipe (suite v9.712)**. Kevin diag mai 2026 : 22 équipes roulettes, chefs BJ mal classés. Deux bugs résiduels après v9.712 (parser préserve `c`) : (1) `_famFromCells` classait TOUT code plain en "roulettes" — or un croupier BJ a EXACTEMENT les mêmes codes plain (`22/6`, `20/5`) qu'un croupier roulette. Seul le CHEF porte `c`. FIX `_famForDetect` : `bj`/`cmc` (signaux fiables `c`/`"`) priment ; pour `roulettes`/plain → déférer à la section PDF (`familyHistory[key]`) qui connaît la colonne d'import. (2) Signature `cycle:offset:firstWork` préservait le suffixe (v9.711) → chef `22/6c` isolé de ses croupiers `22/6` → groupe d'1 filtré → chef perd sa détection. FIX : normaliser le suffixe `[c'"*]+$` dans la signature → chef + croupiers clusterisent. Familles tranchées par `_famForDetect` + split post-groupe. Miroirs diffèrent par horaire de BASE (`20/5` vs `22/6`) → normalisation sûre. NEW flag `cmc_v713_redetect_done`. Tests : **141/141 OK** (test:kevin 10/10, master 97.5%). |
| **v9.702** | **🎯 FIX ROOT CAUSE — "Équipes avril toujours avec nouvelle version"** (Kevin 2026-05-18). Diagnostic : v9.701 wipe-lock protégeait `cmc_ov` mais **2 fallbacks vers `emp.team` (DEF_EMP statique) restaient dans vPlan** (lignes 23454 et 23490). Sans `teamHistory[2026-3]`, vPlan affichait toujours les emps groupés par leur équipe DEF_EMP (= équipe d'avril). FIX : (1) Ma section filter strict `return th===tid` (sans `||e.team===tid`) ; (2) Family iteration `var et=e.teamHistory[key]` (sans `||e.team`) ; (3) `_myTeamId` strict `teamHistory[key]` (sans fallback `emp.team`) ; (4) NEW flag `cmc_v702_wipe_done` pour re-trigger wipe (les devices avec `cmc_v701_wipe_done` déjà set sautaient le re-wipe) ; (5) Wipe IndexedDB shadow (`cmc_e`, `cmc_ov`) — la triple-persistence v9.519 restaurait au boot ; (6) Wipe flags obsolètes `cmc_v696/v698/v701_wipe_done` pour permettre futurs wipes. Tests : test:teamhistory 5/5 + test:kevin 10/10 + test:scoped 10/10 + test:v702 5/5 + badge 5/5 = **35/35 OK**. |
| **v9.692** | **4 topics d'aide v10 ajoutés** dans `HELP_TOPICS` (`v10_admin`, `v10_palette`, `v10_killswitch`, `v10_apercu`) avec explications détaillées de chaque feature (où l'activer, ce que ça fait, astuces). Accessibles via `showHelp("v10_xxx")` ou directement via la palette `⌘K` : 4 nouvelles entrées catégorie "Aide" avec keywords riches (palette, ctrl k, cmd k, kill, switch, widget, apercu…). Bénéfice : un employé ou admin qui ne comprend pas une feature v10 peut taper "aide" + mot-clé dans la palette → guide complet en 2 sec. Cohérent avec règle CLAUDE.md "aide contextuelle ? sur les sections complexes". |
| **v9.691** | **Fix bug navigation broken ref `sv('echanges')`**. Audit des `sv()` après mes ajouts : la suggestion "Échanges de shifts" dans `_searchSuggestionsHTML` (employé) pointait vers une route `'echanges'` inexistante dans le router → fallback silencieux vers vAccueil. CLAUDE.md documente `demanderEchange()` et `adminRepondreEchange()` mais elles ne sont pas implémentées dans le code (probablement feature abandonnée). Remplacé par `sv('mesdemandes')` (vMesDemandes existe au router l. 16107) → suggestion "Mes demandes" qui ouvre la vue valide. Aucune autre route cassée détectée. |
| **v9.690** | **Indicateur "👥 N en ligne" pulsant dans le widget Aperçu admin**. Ajout dans le header du widget : juste à droite du status "Tout à jour" ou "N alertes", un compteur live avec point vert qui pulse `● N en ligne`, cliquable pour ouvrir vOnline. Lit `getOnlineUsers()` (existant v8.91, TTL 5 min). Couleur verte cohérente avec les indicateurs sync. Bonus tooltip admin enrichi sur le bouton ⚙ Admin : "5 catégories : 👥 Équipes · 📅 Planning · 🔒 Sécurité · ⚙ Système · 🤖 IA". |
| **v9.689** | **Sprint 5 — toolbar vIA nettoyée + 4 actions IA dans command palette**. Toolbar vIA passe de 4 boutons admin à 2 (Web Search + TTS, contextuels chat). Boutons retirés : "🔍 Vérifier" et "✕ Clear historique" → désormais via `⌘K` palette. Ajout dans `CMD_PALETTE_ACTIONS` (catégorie "IA") de 4 entrées : Vérifier connexion · Effacer historique chat · Toggle Web Search · Toggle TTS. Keywords riches (recherche, check, clear, voice, parler…) pour découverte fuzzy. Bénéfice : la toolbar reste lisible même sur 375px, et toutes les actions IA sont retrouvables via palette en 3 secondes. |
| **v9.688** | **Widget Aperçu admin enrichi — dernière action audit dans l'empty state**. Quand pas d'alerte active, le widget affiche désormais : "✓ Aucune alerte. Dernière action : motd_set il y a 12 min · Astuce : ⌘K ouvre la palette." Lit le dernier `_audit()` depuis `cmc_audit`, calcule un timestamp relatif FR ("à l'instant", "il y a N min/h/j"). Information à valeur opérationnelle : Kevin sait quand quelqu'un a modifié quelque chose sans cliquer dans le journal. Plus de typo "Lessons partagees" → "Leçons partagées Apex + CMCteams" (correction FR/accents). |
| **v9.687** | **Welcome v10 modal enrichi à 6 features + "Revoir nouveautés" dans Système**. Le modal d'accueil v10 (`_cmcShowV10Welcome`) liste désormais 6 fonctionnalités au lieu de 4 (ajoute "📅 Aperçu admin + mini-calendrier", "🔍 Filtre dans chaque catégorie", "🧭 Fil d'Ariane à 3 niveaux"). Padding réduit pour rester compact. Nouveau item dans `Admin → Système & Configuration > Nouveautés v10` : `📖 Revoir les nouveautés v10` qui efface le flag `cmc_seen_v10_678` et relance le modal. Kevin peut donc revoir les changements quand il veut. |
| **v9.686** | **Breadcrumb enrichi — vue catégorie Admin à 3 niveaux**. Quand l'admin est dans `vAdminCat` (vue détaillée d'une catégorie), le breadcrumb affiche désormais 3 segments : `🏠 Accueil › ⚙️ Administration › 👥 Équipes & Employés` (au lieu de seulement Accueil + catégorie). Les 2 premiers sont cliquables (retour rapide). Le 3e (label de catégorie) est en accent or. Mapping `catTitles` pour les 5 catégories. Bonus : enrichissement du dictionnaire `labels` avec 16 nouvelles vues (employees, documents, echanges, security, importVerif, conflits, quilibre, passation, passations, rgpd, retardataires, memorypro, sentinelhub, mapeditor, voicepresets, debug, statsglobal, statsannuelles) pour breadcrumb cohérent partout. |
| **v9.685** | **Filtre/recherche dans vAdminCat (catégorie admin)**. La vue détaillée d'une catégorie (Système 14 items, Sécurité 9 items, Planning 12 items) affiche maintenant un champ "🔍 Filtrer dans cette catégorie…" si elle contient > 6 items. Filtre en live (oninput), normalisé NFD, ignore accents. Compteur dynamique "N actions sur N · ✕ effacer le filtre". Empty state si aucun résultat : "🔎 Aucune action trouvée pour 'X' — Essaye un autre mot-clé". Filtre auto-reset à chaque ouverture de catégorie via `svAdminCat()`. Helper `searchInput()` existant (v9.1) pour préserver le focus après dc(). Réduit drastiquement le temps pour trouver une action précise dans une grosse catégorie. |
| **v9.684** | **Toast IA désactivée adapté au rôle**. `iaBlockedToast()` affiche désormais 2 messages distincts. **Admin** : "⏸ IA désactivée — tokens préservés. Cliquer sur le badge IA OFF (topbar) ou Admin → IA pour réactiver." (explique où réactiver). **Employé** : "⏸ Assistant IA temporairement indisponible. Contactez l'administrateur si besoin." (pas de demande d'action impossible — évite frustration). Variante "err" du toast pour rester visible. |
| **v9.683** | **Empty states "Aucun planning" distincts admin/employé**. vMonPlanning et vDeparts : si pas de planning importé pour le mois, affichent désormais 2 versions selon rôle. **Admin** : "Aucun planning pour mai 2026" + sous-titre "Importez le PDF SBM…" + gros bouton CTA gradient or `📥 Ouvrir l'import` (44px). **Employé** : "Le planning sera publié dès l'import par l'administrateur. Aucune action n'est nécessaire de ta part." — pas de bouton trompeur (avant : tous voyaient un bouton "→ Importer le planning" qui ouvrait une vue inaccessible). Icône 📋 42px, padding généreux. UX clair, simple, FR : chacun voit ce qui le concerne. |
| **v9.682** | **Bouton ⌘ dans la topbar — palette accessible sur mobile**. Ajout d'un mini bouton ⌘ (30x30) à côté de 🔍 qui appelle `openCommandPalette()` directement. La palette plein écran existante (v9.82) n'était accessible que par raccourci clavier `Ctrl+K`/`Cmd+K`, donc inaccessible sur mobile/tactile. Désormais tous les users (admin et employés) peuvent l'ouvrir d'un tap. Title explicite `"Palette de commandes (toutes actions, vues, raccourcis) · ⌘K"`. Style mono pour le `⌘` (différencie visuellement du 🔍 emoji). |
| **v9.681** | **Mini-calendrier visuel "Couverture mois"**. La barre de progression linéaire est remplacée par un mini-calendrier 1 case/jour : vert si planifié, sombre si non, semi-transparent si passé, **or pulse** si aujourd'hui. Tooltip natif par case (`title="N : planifié/non planifié (aujourd'hui)"`). Légende sous le calendrier (planifié / à venir / aujourd'hui). Kevin voit d'un coup d'oeil quels jours du mois en cours sont encore à importer. Cliquable pour ouvrir vPlan. |
| **v9.680** | **Couverture planning + élimination doublon command palette + suggestions search**. (1) Découverte d'une command palette déjà existante v9.82 (`openCommandPalette()` l. 9094, modal plein écran fuzzy + navigation flèches + ~40 actions `CMD_PALETTE_ACTIONS`). Mon listener Ctrl+K v9.677 + tableau `_CMD_PALETTE_TARGETS` dupliquaient → suppression complète (-3 KB), globalSearch réutilise directement `CMD_PALETTE_ACTIONS`. Bouton 🔍 topbar = recherche rapide dropdown, ⌘K = palette complète. (2) Mini progress bar "Couverture mois" dans widget Aperçu admin : compte les jours du mois ayant au moins 1 employé planifié, affiche `N/totalJours · %` avec barre dégradée selon seuil (rouge <50%, or <90%, vert ≥90%), cliquable pour ouvrir vPlan. (3) Welcome v10 modal (v9.679) + suggestions rapides dans search dropdown quand vide (6 raccourcis selon rôle). |
| **v9.679** | **Welcome v10 + pulse badge IA OFF + nettoyage toolbar vIA**. Modal `_cmcShowV10Welcome()` qui s'affiche 1 fois par admin (flag `cmc_seen_v10_678`) au premier vAccueil après update : présente les 4 nouveautés v10 (Admin refondu · Kill-switch IA · ⌘K · Widget Aperçu) avec icônes et descriptions, CTA "⚙ Découvrir le nouveau Admin" en gradient or → ouvre vAdminV10, bouton "Plus tard" pour fermer. Backdrop blur, animation fadeIn .25s. Badge "⏸ IA OFF" topbar : `animation:pulseDot 2.5s ease-in-out infinite` pour rendre l'état OFF impossible à manquer. Toolbar vIA : bouton ⏸ Désactiver IA retiré (4 endroits suffisent : Admin → IA, widget Aperçu vAccueil, badge topbar, toast). |
| **v9.678** | **Refonte UX Sprint 3 — vAdminV10 par défaut + widget "Aperçu admin" sur vAccueil**. Le router fait désormais pointer `sv('admin')` vers `vAdminV10()` (5 catégories). L'ancien panneau reste accessible via `sv('adminold')` ou le bouton "↩ Classique" en tête de vAdminV10. Nouveau widget `Aperçu administrateur` en haut de vAccueil (admin uniquement) : header "Tout est à jour ✓" ou "N alertes actives", boutons inline ⏸/✅ IA toggle + ⚙ Admin (gradient or), mini-stats horizontales (Conflits, Nouveaux inscrits, Codes pending, Apex pending, Passations, Uploads) avec couleur sémantique par type, chiffres mono JetBrains 20px. Empty state avec astuce `⌘K`. Ne se rend que si admin connecté → invisible pour les 258 employés. |
| **v9.677** | **Refonte UX Sprint 2 — Command Palette ⌘K + nettoyage toolbar vIA**. Raccourci clavier `Ctrl+K` / `Cmd+K` ouvre la recherche globale (style Linear/Vercel). Tableau `_CMD_PALETTE_TARGETS` avec 30+ cibles : navigation vues (Accueil, Planning, Chat, IA, Convention…), actions admin (Stats, Import, Conflits, Passation, RGPD, Sauvegarde…), kill-switch IA, config clé API/proxy. Filtrage admin (`admin:true`) — non-admins ne voient pas les actions sensibles. Recherche enrichie : input `onkeydown` → Entrée lance le 1er résultat, 1er résultat surligné or, dropdown 340px de haut (vs 200), empty state "Aucun résultat pour…". Helper `_searchOnKeydown()`. Toolbar vIA nettoyée : les boutons 🔑 clé API et 🔗 proxy sont retirés (accessibles via Admin → IA & Outils, élimine doublons). Bouton topbar 🔍 a `title="Rechercher (⌘K / Ctrl+K)"`. |
| **v9.676** | **Refonte UX Sprint 1 — Nouveau panneau Admin v10 (5 catégories)**. Nouvelle fonction `vAdminV10()` (l. 29449) accessible via `sv('adminv10')` ou bouton ✨ en tête de l'ancien vAdmin. Structure : raccourcis du jour (4 mini-cards Passation/Historique/Apex/Crossteam) + grille 5 grandes cards de catégories (Équipes & Employés · Planning & Horaires · Sécurité & Audit · Système & Configuration · IA & Outils) + features toggles collapsibles. Helpers : `_adminCatsV10()` mapping centralisé, `_renderAdminItemsV10()` rendu sep/grp avec style v10, `svAdminCat()` navigation, `vAdminCat()` vue détaillée par catégorie. Routes ajoutées : `adminv10`, `admincat`. Hover styles CSS `.adminv10-cat:hover` + `.adminv10-shortcut:hover` avec `prefers-reduced-motion`. **Doublons éliminés** dans le nouveau panneau (Audit & historique imports vs Journal modifications → fusionné · Mode présentation toggle dupliqué → 1 seule entrée). Sprint 2 partiel : boutons 🔑 clé API et 🔗 proxy retirés du toolbar vIA (désormais uniquement dans Admin → IA & Outils, plus de doublon). Zéro risque : l'ancien vAdmin reste accessible par bouton "↩ Classique", aucune route supprimée. |
| **v9.675** | **Kill-switch IA admin global**. Bouton `⏸ Désactiver IA` en tête de `vAdmin` + badge `⏸ IA OFF` topbar permanent quand actif. Helper centralisé `iaIsEnabled()` (l. 37903) consulté par TOUS les sites d'appel Anthropic. Guards ajoutés sur 5 sites non protégés : sentinelle maintenance (l. 11962), vision fallback PDF (l. 26070), OCR scan document (l. 38601), scanner badge (l. 38674), camera studio (l. 38776). Toggle existant `iaToggle()` réutilisé. Coupe 100% des appels Anthropic (avant : seul le chat vIA était gardé, les scans/OCR consommaient toujours). Doc pattern dans `MEMO_KILLSWITCH_IA.md` pour application aux autres projets (Apex AI exclu volontairement). |
| **v9.605** | **[Audit cascade externe]** 5 fixes P0/P1 cascade audit (ROI 141→158/200 estime). Per-user feature toggles (`cmcFeatureEnabledForUser`/`cmcSetFeatureForUser`/`cmcResetFeatureForUser` — regle Kevin "ON/OFF general+individuel"). User-friendly errors (`cmcUserFriendlyError`/`cmcSafeToast` — regle Kevin "JAMAIS message technique brut"). Sentinelle `regression-watch` 24h interval, audit 11 fix critiques proteges (cmc_uid FB_LOCAL strict, cap chat, parser cadres `_parseEncadresStatuts`, V2 merge `_cmcListVersionedHistory`+`_cmcDiffVersions`, esc XSS, BULLETIN_CODES, etc.) + escalade Apex via `_cmcEscalate` si regression. Helper `cmcDiagAuto()` admin 1-clic check sante app (10 checks: Firebase, storage, employees, Convention SBM, parser, V2 merge, helpers, esc, cross-app pipeline). Validation `node --check` PASS sans separateur (CLAUDE.md erreur #29 method). |
| **v9.70** | **Fixes responsive mobile complets**. Tests multi-devices Puppeteer (5 profils : iPhone SE, iPhone 14 Pro, Galaxy S22, Pixel 7, iPad Air). Fix nav bas #bnav : scroll-x interne, labels cachés < 420px (ne garde que les emojis), compact sur petits écrans. Fix overflow accueil (raccourcis `max-width:100vw`). Fix toolbars vIA, vChat header, vMonPlanning header : `flex-wrap:wrap`. Ajout `html,body{overflow-x:hidden;max-width:100vw}` en safety net. Résultat : **70 PASS / 0 FAIL** sur 5 devices (avant : 55 PASS / 25 WARN overflow). iPhone SE 375px entièrement fonctionnel. |
| **v9.69** | **Audit expert 4 subagents parallèles + corrections**. Fix P1 : `cmc_motd` maintenant géré dans `fbApplyData` (accepte null=effacé, validation type objet). Fix P2 : auteur MOTD supprimé affiche "(supprimé)" au lieu de "undefined" ; bandeau MOTD gagne `word-break:break-word` + `overflow-wrap:anywhere` pour textes longs sans espaces. Section **"Outils & réflexes expert"** ajoutée dans CLAUDE.md (boîte à outils, commandes de validation, pièges à éviter). |
| **v9.68** | **Message du jour admin + sync Firebase**. Store `A.motd={text,ts,author}` dans FB_FIX (`cmc_motd`). Fonctions `setMotd`/`clearMotd`/`adminSetMotdFromInput` (guard AID, max 500 car., audit `motd_set`/`motd_clear`). UI admin : textarea + boutons Publier/Effacer. UI employé : bandeau doré 📢 en haut de `vAccueil` (pre-wrap, date/heure). |
| **v9.67** | **Version majeure 35+ fonctionnalités**. Splash screen, Firebase différé, auto-save fiches, CODE_HOURS complet, solde CP, dashboard RH + courbe 12 mois SVG, TTS/STT, compte visiteur U007, thèmes (Casino/Clair/Nuit), export PDF, templates planning, multi-langues FR/EN/IT, swipe mois, admin réorganisé 7 catégories, PWA installable, mode présentation. IA locale enrichie (36 outils). AUDIT 23/23 PASS. |
| **v9.66** | Lisibilité (ratio WCAG AA), tokens CSS typography+blur centralisés, titres serif Garamond. |
| **v9.65** | Cadre légal monégasque (Loi 1.103, OS 8.929, AM 88-384) + vConvention onglet Loi + 2 outils IA supplémentaires. |
| **v9.64** | JEUX_SBM Formation 2016 (6 jeux détaillés : BJ, RA, Craps, PB, THU, TCP) + vConvention Jeux enrichi + 2 outils IA. |
| **v9.63** | Tool use IA custom — 26 outils (21 lecture + 5 admin). IA_TOOLS[] + _iaExecuteTool + guard AID sur les 5 admin. |
| **v9.62** | Multi-axe : bulles chat transparentes + fix iOS zoom + CSP élargie + upload modéré + PLANS_CMC/CDP + galerie 75 photos salons + 15 tests unitaires. |

